# R3 labile 平衡 + P1-1 接线 + 判别力 env —— 落盘与测试（2026-08-18）

> 指令：30 分钟三个欠账（labile/P1-1/env）直接干。分支 `stage-rewrite-20260818`。
> 三件全部落盘，新增测试全绿，全量回归见文末。

---

## 1. labile 平衡（R3 核心——语义决策 D-2026-08-18-01 代码化）

**落盘**：`core/doubt/reconsolidation_queue.py`（新增，纯 stdlib）
- **再巩固候选队列持久化（跨重启）**：写侧入队即落盘（原子写 tmp+rename），
  启动加载；默认路径 `<data_dir>/reconsolidation/candidates.json`
  （`LMS_DOUBT_RECONSOLIDATION_QUEUE_PATH` 可覆盖）。labile 窗口是内存态可失，
  候选队列是持久态不失——窗口=时相观测，队列=再巩固计划。
- **巩固期受控改写（三闸门机器防线）**：`maybe_rewrite` 任一不过 → 不改写
  - G1 `candidate_in_queue`（候选在队）
  - G2 `consolidation_triggered`（巩固期写侧时相才触发）
  - G3 `rewrite_controlled`（rewrite_fn 委托写侧入口 **或** 内部受控动作 =
    append-only 演化史登记 `process_core.append_transition`，写者 consolidation，
    绝不覆盖历史）
- **检索不塑形**：检索时相 `enqueue` 被拒（零副作用）；只读接口
  （contains/peek/candidates/snapshot）绝不 setattr 条目——只读四不变保持。
- 治理开关 `LMS_DOUBT_RECONSOLIDATION_ENABLED` 默认 1；0 → 零参与。
- 注册：`core/doubt/__init__.py`（MODULE_CLAIMS 4 条 + import + __all__）、
  `core/doubt/claims.json`（16 claims）。
- 测试：`tests/test_reconsolidation_queue.py`（27 例：三闸门/检索不塑形/
  持久化重启/受控 append-only/容量 TTL/开关关/损坏文件 fail-open）。

## 2. P1-1 条目级过程字段接线（api/server.py writer 路径 → core.store.ingest）

**落盘**：`api/server.py`
- `/store` 写路径改走 **写侧统一入口** `core.store.ingest`：语义解析 →
  P1-1 过程核心提取 → 幂等查重 → writer 写 → 登记 → 注入时怀疑钩子。
- writer 回调 = 原 S1-1 流程（process_turn + 条目 meta 后处理）；结果 dict
  携带 `entry` 键（P1-1 待集成点接线）——ingest 把 process_core/
  text_snapshot/evolution 附加到条目，随后弹出该键（torch 张量不进幂等
  记录/journal/响应）。
- `StoreResponse` 追加 P1-1 区段：`process_core` / `text_snapshot` / `evolution`
  （独立追加不改既有字段语义）。
- 熔断降级保持 503+Retry-After+degraded：writer 抛 `_StoreEmbedDegraded`
  （**写失败不登记幂等键**——窗口内同 payload 重试可重写，符合 ingest 契约
  "透传 writer 异常不登记"）；同步重活仍走 run_in_executor（不阻塞事件循环）。
- 幂等记录仍 = 纯 writer 结果（core.store 既有契约不破坏）；dedup 命中路径
  同样携带过程字段（确定性产物一致）。
- 测试：`tests/test_store_endpoint.py` 新增 `TestStoreProcessCoreWiring`（2 例：
  响应+条目双双携带过程字段 / 幂等命中路径携带过程字段）。

## 3. 判别力 env（审计 §2.2 阶段三 B 判别力恢复）

**落盘**：`rewrite-ws/.env`（生产环境参数，新增）
- `LMS_DOUBT_SURPRISE_FACTOR=1.25`——门槛 = 1.25×j_target(40) = 50 >
  surprise_mean+2.5σ≈47.8 → 常态条目放行、异常条目仍拦（审计排期表 env 键列）。
- 其余生产键（验证链灰度/双写轮数/目的检查/既有开关）随 .env.example 参数集
  落位；文件头注释引用审计报告 §2.2 排期表。

---

## 测试结果（新增定向）

| 测试集 | 结果 |
|---|---|
| tests/test_reconsolidation_queue.py（新增 27 例） | ✅ 27 passed |
| tests/test_store_endpoint.py（含新增 2 例接线） | ✅ 12 passed |
| tests/test_p1_1_process_core.py | ✅ 24 passed |
| tests/test_store_m1.py | ✅ 9 passed |
| tests/test_purpose_drift.py + test_doubt_m3.py + test_api_server.py + test_discrimination_recovery.py + test_doubt_integration.py + test_doubt_recall_exclusion.py | ✅ 202 passed |
| tests/test_loop_m5 / m7_dual_write / store_m1 / integration / dream_engine / memory / purpose / precision_adapt / persistence | ✅ 232 passed |

全量回归：`pytest tests/ -q`（后台全量）→ **1373 passed, 5 warnings（0:03:08）**
——基线 1344 + 新增 29（labile 27 + P1-1 接线 2），全绿，无回归。
