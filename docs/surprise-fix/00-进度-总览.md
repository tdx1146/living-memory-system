# 惊讶度修复（方案 A：语义拆分）— 进度追踪

> 建立：2026-08-10 17:14 ｜ dandan 拍板：按方法论全流程执行（设计→审计→实施→再审计），全部派遣子 AI
> 依据：《惊讶度科学调研-20260810.md》（母本论文 Spisak & Friston arXiv:2505.22749）
> 原则：严格按论文；不偏离架构；不引入新问题；不瞎改其他模块（契约面：:8190 API / MCP / glue / 回魂快照 / 编辑器）

## 流程状态

| 步骤 | 状态 | 执行者 | 产出 | 结论 |
|------|------|--------|------|------|
| 0. 科学调研 | ✅ 完成 | 子AI-调研 | 惊讶度科学调研-20260810.md | 方案 A（语义拆分） |
| 1. 设计 | ✅ 完成 | 子AI-设计 | 惊讶度修复-01-设计方案.md（425行） | 方案A细化：契约零破坏（字段名全不动，仅值语义变）；发现做梦零精度陷阱+softmax退化两深水区；改动点精确到行 |
| 2. 审计设计 | ✅ 完成 | 子AI-审计 | 惊讶度修复-02-设计审计.md（207行） | 有条件通过 + 8项必改（论文编号v1→v2、C6必做、phase3测试漏网、softmax鲁棒性、glue批次定案等） |
| 2.5 设计修订 | ✅ 完成 | 子AI-修订 | 惊讶度修复-01-设计方案.md v1.1 | 8项全部落实，设计自洽可实施；free_energy 保留原名+全称限定；glue 定案=独立commit G1窗口min-max |
| 3. 实施 | ✅ 完成 | 子AI-实施 + 主控收尾 | C1-C5（LMS）+ G1（glue）6 个 commit | 全量 693 passed（含新增 12 项，153.98s）；C5 提交 e096eda 由主控收尾（子AI超时前已完成代码） |
| 4. 审计实施 | ✅ 完成 | 子AI-审计 | 惊讶度修复-04-实施审计.md | 有条件通过：LMS 5 commit 通过；glue G1 空窗口 min(w) 崩溃（必改）→ 主控修复 1c92b95 → 两仓已推送（LMS e096eda / glue 1c92b95，ls-remote 验证一致） |

## 最终结果（2026-08-10 21:00 闭环）

- LMS 仓 main：`05344c2` C1 / `33210eb` C2(k=0.1) / `4b5f0c2` C3 / `1a5c727` C4 / `e096eda` C5 → 已推送，远端 HEAD=e096eda
- glue 仓 main：`8b2d7e3` G1 / `1c92b95` G1 空窗口修复 → 已推送，远端 HEAD=1c92b95
- 全量测试：LMS 693 passed（原 681+新增 12）；glue 66 passed（2 失败为环境既有问题，与修复无关）
- 实测验证：surprise≥0 ✅ 分解自洽差 0 ✅ 生产快照 ρ(surprise,FE)=−0.214（能量污染剥离证据）✅ 单离群权重 0.183 ✅ sigmoid 非饱和 ✅ 旧快照/API 契约兼容 ✅
- 遗留建议项（非阻塞）：C5 message 补记 phase3 裁定、verify 脚本合成网络 ρ≈1 备注、glue 补窗口退化单测、看门狗 T1.4 来源标注

## 设计约束（传递给所有子 AI）

1. **论文对齐**：surprise = −ln P(o) 恒≥0 是报告语义；VFE 是上界；能量泛函可负仅作学习目标（调研报告第三章 A）
2. **架构对齐**：不改五组件职责（Embedder 冻结 / AttractorNetwork FEP / PurposeLayer precision / MemoryManager EMA / Tokenizer 动态词表）
3. **契约面零破坏**：:8190 /status 返回字段、MCP 工具返回、glue /recall 的 lms 结果、回魂快照——字段名/格式变更必须有兼容策略（新增字段优先，不删除旧字段）
4. **范围控制**：只改惊讶度语义相关（attractor/purpose/memory/dream_engine/meta 五处），严禁顺手改其他模块
5. **验证**：681 测试全绿；新增验证（accuracy vs F 排序差异对比）
6. **回滚**：每步可回滚（git 提交点）

## 工程化硬约束（2026-08-10 17:32 dandan 追加，实施阶段必须遵守）

1. **代码质量**：遵循现有代码风格；docstring 写明语义；不留死代码/调试残留
2. **模块独立**：高内聚 + 低耦合 + 咬合顺畅；只动惊讶度语义相关五处核心，不越界
3. **路径指定**：禁止硬编码绝对路径；用 Path(__file__) 推导或配置项（沿用脱敏后的自动推导规范）
4. **审计串联**：实施前设计审计结论已落实；实施后必须再过审计（第 4 步）
5. **结果纳入版本管理**：每批 C1→C5 独立 commit（禁合并提交）；设计/审计/进度文档一并提交到 agent-os 或 workspace；代码 commit 到 living-memory-system main
6. **测试门槛**：每批落地后跑冒烟（test_attractor + test_integration + phase3_emergence slow）；最终全量 681 测试
7. **契约兼容**：API/MCP/glue/回魂/self_pulse 消费方字段名不动，仅值语义变；glue 微调为独立仓库独立 commit


## 关键决策记录

- 2026-08-10 17:14：dandan 指示——按方法论调子 AI，先设计→审计→实施→再审计，文本记录进度，全程子 AI 执行
- 方案选择：A（语义拆分：surprise=准确性项恒≥0；free_energy=现有 F 可负；移除 F/‖J‖；J 钳制解耦无条件生效；下游四处改语义）
