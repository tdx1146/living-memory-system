# 体验层实施 A-D（2026-08-11）

按《体验层总设计-20260811.md》v1.1 完成四阶段实施（dandan 已整体批准）。

## 提交（9 个，跨 4 仓，禁合并提交）
| 阶段 | 仓库 | commit | 内容 |
|------|------|--------|------|
| A | LMS | a38cc53 | /react infer-only + 解读段 + 插件三路并行 + 限流轻量注入 |
| A | glue | 822b4da | /react 薄代理 |
| A | agent-os | cd83d29 | C-16 + LM-04 |
| A | 插件 | 534afc4 | 三路并行 + INJECTED-light + 截断保活 |
| B | LMS | 6d6540e | 垃圾过滤 +6 正则（子代理样板） |
| C | LMS | a68f9c7 | _meta_adjust argmin→argmax（强化已关注） |
| D | LMS | 696241e | core/doubt/ 6 文件 + memory/dream/loop 改造 |
| D | glue | e8a6237 | 召回置信度注解 |
| D | agent-os | f568333 | C-13/14/15 + LM-05/06/07 |

## 验证
- 全量 pytest：**740 通过 0 失败**（含 slow phase3 长稳）
- e2e 实测（8192 测试实例）：/react 零持久化（turn/episodic 四不变）；
  [doubt] conflict → 证伪(conf 0.5, labile) → 做梦 → doubt_review rewritten=1；
  /status doubt 字段 + /recall 置信度注解；glue→LMS 全链路
- 红线复核：attractor.py 零 diff；doubt-system 四脚本零改动；心跳跳过未动

## 遗留 / 部署待办（主控接手）
1. **生产 LMS 8190 还是旧代码**：/react、过滤、目的翻转、怀疑链要重启生产
   LMS（.env source + 重启 8190）才生效；当前 glue 19000 已跑新代码，
   /react 会 502 fail-open（插件优雅降级）
2. **插件代码已改但 gateway 未重载**：memory-recall.js 新逻辑需 gateway
   重载插件才生效（index.js 的 8/8 心跳修复仍未提交，与本任务无关）
3. **agent-os 未 push**：C-11 契约红（本地 cd83d29 ≠ 远端）；glue/LMS/插件
   三仓也未 push（push 属外部动作，未擅自执行）
4. **EpisodicEntry 实际在 memory.py**（设计 §8.4 写 types.py——已按实证落点实现）
5. **doubt_review 在 MVP 做梦路径也跑**（工程决策：默认 full_cycle=false，
   保证复核可达；设计只写了 cycle 阶段）
6. **salience_gate.py 加了 SG_DOUBT_FEED 怀疑缺口通道**（默认关，边沿触发）——
   该目录非 git 仓库，改动已备份 .bak-exp-layer 但无法 commit
7. 旧子代理样板已存在于生产 episodic/归档（入口过滤防新增，不清旧）
8. glue 测试 2 个既有失败（fetch_context 断言 vs recall_data、向量网络测试）——
   与本次无关的存量问题

## 关键文件
- 备份：各仓 *.bak-exp-layer（gitignore 已盖）
- 测试：tests/test_react_readonly.py / test_garbage_filter.py /
  test_purpose_meta_flip.py / test_doubt_integration.py（新）
