# LMS 修复方案（2026-08-23）

> 依据：`docs/audit_issues_20260823.md`（三代理并行审计，54 项待修 + 2 项已修复确认）。
> 本文件是修复工作流的**实施蓝图（单一事实来源）**：每条 = 改哪个文件哪一段 + 怎么改 + 注意事项 + 对应 issue 编号。
> 状态标记：`[待实施]`（未动手） / `[已实施]`（本轮改完并经 py_compile/单测） / `[需批准]`（血管级，设计但不实施，待 dandan 批准） / `[已修复]`（08-22 已确认，仅存档）。
>
> **红线纪律（所有修复代理必须遵守，摘自审计清单头）**：
> 1. 血管不换：endpoint signatures（/chat /feed /store /recall /status /landscape /snapshot /restore /dream /e3/review /sessions 等现有签名）、six-layer injection、沙漏 path、wake chain —— **一律不动**；需要新增端点或改签名的修复 → 标记 `[需批准]`，设计进方案但不实施。
> 2. `.env` 红线参数不动：LMS_J_TARGET_NORM=12、LMS_BIAS_SCALE=0.9、LMS_BIAS_ADAPT=1 等（dandan 拍板参数）。本方案所有改动**只改代码不碰 .env 值**。
> 3. 每修改一个文件：先备份 `cp file file.bak-$(date +%Y%m%d%H%M)`，改完 `python -m py_compile <file>` 验证。
> 4. 修复必须带注释：注明对应 issue 编号 + 为什么这么修。
> 5. 全部改完：`cd /vol2/1000/AI专用/living-memory-system-cloud && git add -A && git commit -m "fix(模块): ..."`（建议按模块分批 commit：fix(core) / fix(api) / fix(ops)）。
> 6. 不重启服务、不做破坏性操作；读代码用 read，改代码用 edit/write。
> 7. 修复后跑相关单测（tests/ 目录），不修改测试文件（除非新增断言——审计 A1/A4 明确要求补断言）。
>
> **实施顺序（按审计"修复优先级"）**：
> C1（备份止血）→ A1（内在自愈根因）→ C2/C4/C5（watchdog 循环与毒蛋）→ B1/B2（并发/跨会话）→ A2/A3 → 其余按严重度。
> **需 dandan 批准**：C3（新增 /reset-sigma 端点，血管级）——设计见下，获批前 watchdog 侧只告警不写盘。

---

## 1. 已完成条目（08-22 确认，不需再动，仅存档）

| Issue | 内容 | 状态 |
|---|---|---|
| — | lms_status 嵌套读取修复（审计确认 08-22） | `[已修复]` |
| — | fok 去重修复（`gap_registry.register_fok_unresolved` 归一化去重，08-22 实证：机械援军 3 形态/3080 2 形态去重命中） | `[已修复]` |

---

## 2. 模块 A：核心引擎（core/hippocampus/ + runtime/loop.py + runtime/bus_events.py + core/doubt/）

### [A1][5] `core/hippocampus/attractor.py:262,283-288` | allostatic J 饱和/崩塌重锚 UnboundLocalError 被 fail-open 吞掉（设定点永不更新）
- **状态**：`[待实施]`（P0，最高优先之一）
- **文件/段落**：`core/hippocampus/attractor.py` → `AllostaticJController.update()`（L254-301）
- **根因**：`z` 只在 surprise 统计带 else 分支（L262）赋值；`sat`/`col` 分支（L254-257）命中后走到 L283-288 的 `logger.info(...z=%.2f...)` 时 `z` 未定义 → 必抛 UnboundLocalError（`abs(new_target-j_target)>1e-9` 在 step=0.5 时必成立 → 必进）。loop.py:766-768 broad-except 吞掉 → L299 `self.j_target = new_target` 永不执行 → **σ 饱和/崩塌时设定点永不更新**（σ 饱和长期无法自愈的根因候选）。
- **怎么改**：
  - 在 `if sat:`（L254）之前加一行 `z = 0.0`（sat/col 分支的 z 无统计意义，仅用于日志，0.0 中性即可）。
  - 注释：`# [A1] z 须在 sat/col 分支前初始化：否则 L287 logger 引用未定义 z → UnboundLocalError 被 fail-open 吞掉，j_target 永不重锚（σ 饱和无法自愈根因）`
- **注意事项**：
  - 保持 fail-open 语义不变（loop.py:766-768 的 broad-except 不动）；本修复是消除根因，不是改降级契约。
  - 可选加固：loop.py:766-768 的 except 加 `logger.warning(...)` 带异常信息，便于未来同类错误可见（不改变行为）。
  - 与 dandan 拍板的冷启动门（L251-253 注释）不冲突：只补 z 初始化。
- **验证**：`tests/test_allostatic_j.py` 新增断言（允许新增断言）：构造 `SigmaStats(frac_gt0_9=0.95, act05=200)` 注入 → `update()` 返回的 `j_target` 递减（`- step`）且不抛异常。

### [A2][4] `runtime/bus_events.py:209-219` | CircuitBreaker.status() 持锁内二次 acquire 同一把锁 → 自死锁
- **状态**：`[待实施]`
- **文件/段落**：`runtime/bus_events.py` → `CircuitBreaker.status()`（L209-219）与 `is_open()`（L199-201）
- **怎么改**：status() 内**不再调用 is_open()**，内联判定：
  ```python
  def status(self) -> dict:
      with self._lock:
          return {
              ...
              "open": self._open_until > 0.0 and time.time() < self._open_until,  # [A2] 内联，避免锁内二次 acquire 自死锁
              ...
          }
  ```
  （二选一也可：`_lock` 改 `threading.RLock()`；但内联更明确，推荐内联。）
- **注意事项**：`is_open()` 对外契约不变；status() 是 /status 观测与 quick_test 的接线点，修复后即不挂线程。
- **验证**：`tests/test_circuit_degradation.py`（跑既有用例）；新增断言：`record_failure` 打满后 `status()["open"]` 为 True 且调用不超时（新增断言允许）。

### [A3][4] `core/hippocampus/memory.py:683-695` | `_recall_episodic_scored` 对 top-k 截取**之前**的全部命中执行 record_reference
- **状态**：`[待实施]`
- **文件/段落**：`core/hippocampus/memory.py` → `_recall_episodic_scored()`（L656-695）
- **根因**：L683-690 在排序截取（L692-695）之前对 `scored`（全部相似命中，含从未进 LLM context 的）执行 `record_reference` + 刷新 `last_reinforced_turn` → 每轮给整批条目虚增引用/置信度/磨损计时 → 置信度趋近 1.0、E3/labile 指标失真。
- **怎么改**：把引用加固循环移到排序截取**之后**，只对 `scored[:k]` 生效：
  ```python
  # 按相似度降序取 top_k
  k = min(top_k, len(scored))
  scored.sort(key=lambda x: x[0], reverse=True)
  # [A3] 只对真正进入 top_k 的条目加固：先截取后加固，
  # 避免给整批相似命中（含未进 LLM context 的）虚增 reference/last_reinforced_turn
  if count_reference:
      for _score, entry in scored[:k]:
          record_reference(entry)
          if reinforce_turn is not None:
              try:
                  entry.last_reinforced_turn = reinforce_turn
              except Exception:
                  pass
  return scored[:k]
  ```
- **注意事项**：`record_reference` 的 fail-open 包裹（try/except）保留；顺序变化不影响相似度排序结果，只影响加固范围。
- **验证**：`tests/test_memory.py` / `tests/test_recall_m2.py` 跑既有用例；新增断言：检索 top_k 后，只有前 k 条目 `reference_count` 增加（允许新增断言）。

### [A4][3] `core/doubt/gap_registry.py:226-231 vs 148-155` | mark_resolved 精确匹配，register 模糊去重 → 同悬案换包装无法消解
- **状态**：`[待实施]`（与 B7 同根，一并修复）
- **文件/段落**：`core/doubt/gap_registry.py` → `mark_resolved()`（L202-232）、`is_resolved()`（L234-238）、`register_fok_unresolved()`（L136-162）
- **根因**：登记侧用 `_fok_core` + `_fok_similar`（前缀≥12字/Jaccard≥0.4）模糊去重；消解/判重侧用原始 topic 精确相等（L218-231、L236-238）→ 同一悬案换包装登记后永远无法被消解移除，E3 反复选中已闭环主题。
- **怎么改**：新增统一判定助手，三处共用（与登记侧同构）：
  ```python
  def _same_case(self, a_topic: str, b_topic: str) -> bool:
      """[A4][B7] 消解/判重与登记侧同构的归一化判定（_fok_core + _fok_similar）。"""
      ca, cb = self._fok_core(a_topic), self._fok_core(b_topic)
      return bool(ca) and bool(cb) and self._fok_similar(ca, cb)
  ```
  - `mark_resolved()`：`self._fok_unresolved = [r for r in ... if not self._same_case(r.get('topic',''), normalized)]`；`_fok_resolved` 的幂等去重同样改用 `_same_case`。
  - `is_resolved()`：`return any(self._same_case(topic, r.get('topic','')) for r in self._fok_resolved)`
  - `register_fok_unresolved()` 的循环保持现状（已用同构判定），或改为调用 `_same_case` 统一（推荐）。
- **注意事项**：`_fok_core` 剥"用户:/助手:"与 `[doubt]` 前缀；`_fok_similar` 已含精确相等短路，不会误并独立悬案（实测共享 ≤7 字不误并）。B7 条目由本修复一并覆盖，不再单独实施。
- **验证**：`tests/test_doubt_m3.py` / `tests/test_doubt_integration.py`；新增断言：登记 "旧结论说等待只是被动…" 的变体包装后 `mark_resolved(原始topic)` 能移除 A 类缺口（允许新增断言）。

### [A5][3] `core/hippocampus/attractor.py:338-341` | snapshot() 浅拷贝后原地改写 → 历史事件 ts_turn 永久丢失
- **状态**：`[待实施]`
- **文件/段落**：`core/hippocampus/attractor.py` → `AllostaticJController.snapshot()`（L327-341）
- **怎么改**：
  ```python
  events = [dict(ev) for ev in self.events]   # [A5] 深拷贝：list() 只拷引用，原地改写会污染 self.events 的历史 ts_turn
  ```
- **注意事项**：`dict(ev)` 一层拷贝足够（事件 dict 的值都是标量/None）；不改 snapshot 返回结构（血管）。
- **验证**：`tests/test_allostatic_j.py` 新增断言：`snapshot(turn_count=N)` 后再 `snapshot()`（不带 turn）历史事件 `ts_turn` 保持 None/原值（允许新增断言）。

### [A6][3] `core/hippocampus/attractor.py:1336-1342,1121` | set_landscape 恢复 j_target 不回写 j_target_norm
- **状态**：`[待实施]`
- **文件/段落**：`core/hippocampus/attractor.py` → `set_landscape()`（L1320-1342）；learn() 范数钳制读 `self.j_target_norm`（L1121）
- **根因**：恢复 allostatic.j_target 但 `self.j_target_norm` 仍是 stale 值（可能停 40）→ 恢复后 learn() 范数钳制用旧设定点，饱和态下恢复的低设定点不生效。
- **怎么改**：在 set_landscape 恢复 j_target 之后同步：
  ```python
  if self.allostatic is not None:
      _as = landscape.get("allostatic") or {}
      _jt = _as.get("j_target")
      if isinstance(_jt, (int, float)) and _jt > 0:
          self.allostatic.j_target = max(self.allostatic.j_min,
                                         min(self.allostatic.j_max, float(_jt)))
          self.j_target_norm = self.allostatic.j_target   # [A6] 同步范数钳制设定点，恢复后 learn() 立即按低设定点执行
  ```
- **注意事项**：allostatic 关闭（None）时行为不变（j_target_norm 保持配置固定值）；`j_target_norm` 的唯一在线写者是 update_allostatic（L1379），此处是恢复路径的补充写，不冲突。
- **验证**：`tests/test_allostatic_j.py` / `tests/test_persistence.py` 新增断言：恢复含 allostatic.j_target=7 的快照后 `attractor.j_target_norm == 7`（允许新增断言）。

### [A7][3] `runtime/loop.py:1815-1830` | `_manage_entropy` 同轮第二次完整 learn
- **状态**：`[待实施]`
- **文件/段落**：`runtime/loop.py` → `_manage_entropy()`（L1789-1832）；`core/hippocampus/attractor.py` → `learn()`（L1000 起附近）
- **根因**：极端熵轮次 `_manage_entropy` 用同样 activation 二次调用 `attractor.learn`（L1818/1826）→ J 被更新两次、Hebbian 重复强化，违反"每轮 learn 一次"契约（M5 生命周期）。
- **怎么改**（推荐方案：给 learn 加纯权重校正模式）：
  - `attractor.learn(..., entropy_correction: bool = False)` 新增内部参数（非端点签名，红线允许）：`entropy_correction=True` 时只执行带 `orth_weight_override` 的权重更新段（Hebbian + 正交化 + 范数钳制），**跳过** surprise 统计/allostatic 更新/回放等语义副作用。
  - `_manage_entropy()` 两处调用改传 `entropy_correction=True`。
  - 注释：`# [A7] 熵管理只做正交化增量校正，不做完整 learn——M5 契约"每轮 learn 一次"；第二次 learn 会让 J/He 双重强化`
  - 实施时先通读 `learn()` 内部结构，把副作用段用 `if not entropy_correction:` 包裹（若 learn 副作用本就集中在尾部，改动面很小）。
- **注意事项**：备选方案（并入主 learn 的参数调制）改动更大，不推荐；本方案保持 `_manage_entropy` 调用点语义不变。
- **验证**：`tests/test_attractor.py` / `tests/test_loop_m5.py` 跑既有用例；新增断言：entropy_correction 模式不改变 surprise 窗口长度（允许新增断言）。

### [A8][2] `core/hippocampus/dream_engine.py:452-562` | dream_cycle 七阶段从不调用 memory.consolidate()
- **状态**：`[待实施]`
- **文件/段落**：`core/hippocampus/dream_engine.py` → `dream_cycle()`（L452-562）
- **根因**：dream_mvp（L403）调 `self.memory.consolidate()`，dream_cycle 不调 → full_cycle 模式短时→长时迁移缺失，与 docstring"阶段1 NREM巩固"不符。
- **怎么改**：在周期收尾 `self._doubt_review()`（L524）之后、`self._save_snapshot()`（L527）之前补：
  ```python
  # [A8] full_cycle 补齐短时→长时迁移（对齐 dream_mvp L403；fail-open）
  try:
      self.memory.consolidate()
  except Exception as e:
      logger.warning("dream_cycle 记忆巩固失败（fail-open）: %s", e)
  ```
- **注意事项**：consolidate() 幂等且无参（memory.py:297）；不改变返回结构。
- **验证**：`tests/test_dream_consolidation_m6.py` / `tests/test_dream_engine.py` 新增断言：full_cycle 后 `long_term_latent` 含短时迁移痕迹（允许新增断言）。

### [A9][2] `core/hippocampus/memory.py:55,472-476` | `_GARBAGE_FILTERED` 从不自增
- **状态**：`[待实施]`
- **文件/段落**：`core/hippocampus/memory.py` → 入口垃圾过滤（L470-476）与模块计数器（L55）
- **怎么改**：
  ```python
  try:
      if _is_garbage_text(text):
          global _GARBAGE_FILTERED        # [A9] 计数器声明可观测但从不自增 → 补计数
          _GARBAGE_FILTERED += 1
          return
  except Exception:
      pass
  ```
  可选：在 `get_state()` 输出追加 `'garbage_filtered': _GARBAGE_FILTERED`（追加键向后兼容，status 即可读取）。
- **注意事项**：进程内计数器（重启清零）符合原注释语义；不改变过滤行为。
- **验证**：`tests/test_garbage_filter.py` 新增断言：喂入垃圾文本后计数器 +1（允许新增断言）。

### [A10][2] `runtime/loop.py:2704-2771` | load_state 不恢复 last_activation → 重启后首轮 "[无记忆]"
- **状态**：`[待实施]`
- **文件/段落**：`runtime/loop.py` → `save_state()`（L2595-2657）/ `load_state()`（L2704-2771）；`persistence/snapshot.py` → `save()`（L321-431）/ `load_raw()`（L520）
- **怎么改**（内部可选参数，非端点签名，红线允许）：
  - `persistence/snapshot.py::save()` 新增可选参数 `last_activation: Optional[dict] = None` → `if last_activation is not None: data['last_activation'] = last_activation`（放在 L396 last_entropy_ratio 之后）。
  - `loop.py::save_state()` 序列化：
    ```python
    last_act_state = None
    if self.last_activation is not None:
        la = self.last_activation
        last_act_state = {
            'state': la.state.detach().cpu().tolist(),
            'entropy': float(la.entropy), 'surprise': float(la.surprise),
            'free_energy': float(la.free_energy), 'mse': la.mse,
            'per_dim_surprise': (la.per_dim_surprise.detach().cpu().tolist()
                                 if la.per_dim_surprise is not None else None),
        }
    # 传入 snapshot.save(..., last_activation=last_act_state)  # [A10] 双侧补 last_activation 状态
    ```
  - `loop.py::load_state()` 恢复（放在 tokenizer/meta 恢复段之后）：
    ```python
    la_state = raw_data.get('last_activation')
    if isinstance(la_state, dict) and la_state.get('state'):
        from core.types import Activation
        self.last_activation = Activation(
            state=torch.tensor(la_state['state'], device=self.attractor.device),
            entropy=float(la_state.get('entropy', 0.0)),
            surprise=float(la_state.get('surprise', 0.0)),
            free_energy=float(la_state.get('free_energy', 0.0)),
            mse=la_state.get('mse'),
            per_dim_surprise=(torch.tensor(la_state['per_dim_surprise'])
                              if la_state.get('per_dim_surprise') is not None else None))
        logger.info("last_activation 已恢复（重启后 query_llm 不再首轮 [无记忆]）")  # [A10]
    else:
        logger.info("快照不含 last_activation 字段，跳过（向后兼容）")
    ```
- **注意事项**：旧快照无该键 → 优雅跳过；`query_llm`（L2576）与 get_status（L2785-2792）读取路径自动受益。
- **验证**：`tests/test_persistence.py` / `tests/test_self_ref_persistence.py` 新增断言：save→load 后 `loop.last_activation is not None` 且 state 形状一致（允许新增断言）。

### [A11][2] `persistence/recovery.py:302-311,345-377` | 恢复失败回滚快照不含 allostatic 字段
- **状态**：`[待实施]`
- **文件/段落**：`persistence/recovery.py` → `_snapshot_current_state()`（L283-343）与 `_rollback()`（L345-377）
- **根因**：回滚快照只存 J/bias/sigma + purpose + memory，不含 allostatic → 回滚经 `set_landscape`（L203-204）无法复原已被改写的 allostatic.j_target。
- **怎么改**：`_snapshot_current_state()` 的 attr_state 追加：
  ```python
  allostatic = getattr(attractor, 'allostatic', None)
  if allostatic is not None:
      attr_state['allostatic'] = {
          'j_target': getattr(allostatic, 'j_target', None),
          'j_history': list(getattr(allostatic, 'j_history', [])),
      }   # [A11] 回滚快照补 allostatic：set_landscape 恢复 j_target 时需要
  ```
  （`_rollback` 无需改动：`_restore_attractor` → `set_landscape` 已读 `landscape.get("allostatic")`；可顺带在 set_landscape 补 `j_history` 恢复，可选。）
- **注意事项**：`j_target` 为标量、`j_history` 为 float 列表，深拷贝无共享引用问题；allostatic 为 None（开关关）时跳过。
- **验证**：`tests/test_persistence.py` 新增断言：构造恢复失败场景，回滚后 `attractor.allostatic.j_target` 复原（允许新增断言）。

### [A12][2] `runtime/loop.py:1421` | E3 路径 B 把重激活线索当普通 external 条目写入
- **状态**：`[待实施]`
- **文件/段落**：`runtime/loop.py` → `_e3_reactivate_path_b()`（L1410-1426）；`process_turn()`（L558）与 `store_episodic` 调用（L887-890）
- **根因**：`process_turn(clue)` 把线索文本以 `source='external'`（L890）写入 episodic → 探针文本污染记忆、可被后续检索注入、自喂风险。
- **怎么改**：
  - `process_turn()` 签名加可选参数：`def process_turn(self, user_input: str, llm_output: str = "", source: str = 'external') -> str`（内部方法签名，红线允许），store_episodic 传 `source=source`。
  - `_e3_reactivate_path_b()`：`self.process_turn(clue, source='doubt')`。
  - 注释：`# [A12] E3 路径 B 线索以 doubt 来源入库：L1 检索 source_filter='external' 排除，不污染/不自喂；FEP surprise 仍照常计算（去稳定化不依赖落库）`
- **注意事项**：`[doubt] 前缀解析（doubt_ingest）只针对 user_input，与 source 参数正交；source='doubt' 不触发 is_doubt_event 分支（仍走普通塑形），仅改变落库来源标记。各现有调用点（/chat /feed /store /dream）不传 source → 默认 'external'，行为不变。
- **验证**：`tests/test_e3_reactivation.py` 新增断言：路径 B 触发后，新条目 `source == 'doubt'`（允许新增断言）。

### [A13][2] `core/hippocampus/attractor.py:857,941` | infer docstring 与实现温度标定不符
- **状态**：`[待实施]`（主方案零行为变化；备选 [需批准]）
- **文件/段落**：`core/hippocampus/attractor.py` → `infer()`（L847-948）
- **怎么改**（推荐：改注释，实现为准，零行为变化）：
  - L857 docstring 由 `sigma += sqrt(2*T) * noise` 改为 `sigma += T * noise  # 实现为准（randn_like * temperature，L941）；物理 FEP 标准形式为 sqrt(2T)，此处按现有标定实现`。
  - 注释：`# [A13] 注释与实现统一（以实现为准，避免误导后续标定）`
- **注意事项**：备选方案（实现改 `* math.sqrt(2.0 * temperature)`）会改变噪声强度 → 温度标定/做梦动力学行为变化 → 标 `[需批准]`，若 dandan 要求物理正确性再实施（同时需回归测试全部做梦/attractor 用例）。
- **验证**：py_compile + 跑 `tests/test_attractor.py`（注释改动无行为影响）。

### [A14][2] `core/hippocampus/dream_engine.py:918-924,1025-1027` | `_entry_id` 无 id 回退 turn → 多条目共享同键
- **状态**：`[待实施]`
- **文件/段落**：`core/hippocampus/dream_engine.py` → `_entry_id()`（L918-924），`_value_replay()`（L1025-1033, L1043-1046）
- **根因**：`_entry_id` 在无 id 时回退 `turn`；supersedes 记录复用原条目 turn → 多条目共享同键 → `score_map`/`replay_set_ids` 分数错配/ID 歧义（episodic 条目当前无 id 属性 → 全部回退 turn）。
- **怎么改**：`_entry_id` 优先 id → 文本哈希 → turn 兜底：
  ```python
  @staticmethod
  def _entry_id(entry):
      """[A14] 条目标识：id 优先；无 id 用文本 sha1 前 12 位（同 turn 多条目不撞键）；text 空才回退 turn。"""
      eid = getattr(entry, 'id', None)
      if eid is None:
          text = getattr(entry, 'text', '') or ''
          if text:
              eid = 't:' + hashlib.sha1(text.encode('utf-8')).hexdigest()[:12]
          else:
              eid = getattr(entry, 'turn', None)
      return eid
  ```
  （文件头补 `import hashlib`；调用点不变。）
- **注意事项**：`replay_set_ids`/`reinforced_ids` 输出字符串键，进 dream_state.json 观测无类型破坏；supersedes 复用原条目 turn 不再撞键。
- **验证**：`tests/test_dream_value_replay.py` 新增断言：同 turn 两条不同文本条目 `_entry_id` 不同（允许新增断言）。

### [A15][1] `core/hippocampus/purpose.py:307-308` | `_meta_adjust` 注释与语义矛盾
- **状态**：`[待实施]`
- **文件/段落**：`core/hippocampus/purpose.py` → `_meta_adjust()`（L307-308）
- **怎么改**：L307 注释改为：
  ```python
  # [A15] 强化已关注方向：将历史窗口内平均 precision 最高的维度设为最大值
  # （meta-flip 强化，非"探索新方向"——语义与实现一致）
  ```
- **注意事项**：只改注释，代码不动。
- **验证**：py_compile + 跑 `tests/test_purpose_meta_flip.py`（无行为变化）。

### [A16][1] `runtime/loop.py:616,1062,2549-2555` | 存储文本前缀不一致 + 快照命名 turn 比内容 +1
- **状态**：`[待实施]`
- **文件/段落**：`runtime/loop.py` → process_turn 文本构造（L616）、`_auto_snapshot()`（L2541-2558）、`save_session_state()`（L2659-2695）
- **怎么改**：
  1. **前缀统一**（L616）：
     ```python
     # [A16] 统一存储文本前缀：无论 llm_output 是否为空都带"用户:"前缀（旧实现只在非空时加，格式漂移）
     text = f"用户: {user_input}"
     if llm_output:
         text += f"\n助手: {llm_output}"
     ```
  2. **快照命名 turn 对齐内容**：`save_session_state()` 加可选参数 `turn: Optional[int] = None`（默认 `self.turn_count`，现有调用点不变）；`_auto_snapshot()` 改调 `self.save_session_state(turn=self.turn_count - 1)`（`turn_count>0` 时）——因为 `_auto_snapshot` 在 `_increment_turn()`（L1062）之后运行（L1091），此时 turn_count 已 +1，而条目内容用的是增量前 turn。
     - 注释：`# [A16] 自动快照用增量前 turn 命名：条目 turn=增量前，快照名不应 +1`
- **注意事项**：/snapshot 端点、做梦快照、watchdog dispose 的 `save_session_state()` 不传 turn → 保持现状；前缀统一后，llm_output 为空的轮次存储文本多了"用户: "前缀——若有测试断言旧格式需同步更新该断言（红线允许新增/改断言，先跑 tests/test_memory.py、tests/test_store_endpoint.py 确认影响面）。
- **验证**：py_compile + 跑 `tests/test_store_endpoint.py` / `tests/test_loop_m5.py`；新增断言：`process_turn("x")` 后条目文本以 "用户: " 开头（允许新增断言）。

---

## 3. 模块 B：API 与外围（api/ + core/doubt/ + tools/ + bridge/）

### [B1][4] `api/server.py:716-754` | /chat/simple 绕过做梦互斥锁
- **状态**：`[待实施]`
- **文件/段落**：`api/server.py` → `chat_simple()`（L716-754）
- **根因**：无 register_session + acquire/release_conversation；`loop.query_llm` 内部 process_turn 是写路径 → 可与后台做梦并发写同一脑（J/precision/episodic 撕裂）。
- **怎么改**：镜像 /chat（L666-705）的协调段：
  ```python
  sm = get_session_manager()
  scheduler = get_dream_scheduler()
  loop = sm.get_or_create(req.session_id)
  scheduler.register_session(req.session_id)
  acquired = scheduler.acquire_conversation(req.session_id)
  scheduler.touch(req.session_id)
  if not acquired:
      raise HTTPException(status_code=503, detail="系统正在做梦（记忆巩固中），请稍后重试。")  # [B1] 与 /chat 同款做梦互斥
  try:
      response_text = await asyncio.get_event_loop().run_in_executor(
          None, lambda: loop.query_llm(req.user_input))
      ...
  finally:
      scheduler.release_conversation(req.session_id)
  ```
- **注意事项**：响应结构/端点签名不变；`bridge is None` 的 503 分支保留在 acquire 之前（避免无谓等待）。
- **验证**：`tests/test_api_server.py` 新增断言：/chat/simple 在模拟做梦占用时返回 503（允许新增断言）。

### [B2][4] `api/session_manager.py:107-110` | 启动自动恢复把全局 latest.pt 当任意会话候选 → 跨会话污染
- **状态**：`[待实施]`
- **文件/段落**：`api/session_manager.py` → `_try_auto_restore()`（L91-143，候选构建 L107-110，探测 L117-120）
- **根因**：`snap_dir / "latest.pt"`（旧格式全局）被当作任意会话候选；`load_state` 对 session_id 不一致仅告警（loop.py:2766-2769）→ 跨会话污染/静默回退过期脑。
- **怎么改**：探测候选时校验归属：
  ```python
  st = torch.load(path, map_location="cpu", weights_only=False)
  owner = st.get("session_id") or (st.get("meta") or {}).get("session_id")
  if owner and str(owner) != str(session_id):
      logger.warning(f"[{session_id}] 候选快照 {path} 归属会话 {owner}，跳过（防跨会话污染）")  # [B2]
      continue
  ```
- **注意事项**：新规范 `latest_{sid}.pt` 天然按会话隔离，不受影响；旧快照无 session_id 字段 → 放行（向后兼容，保留旧格式恢复能力）。
- **验证**：`tests/test_api_server.py` / `tests/test_persistence.py` 新增断言：跨会话快照不被自动恢复选中（允许新增断言）。

### [B3][3] `api/server.py:1354-1403` | /restore 不校验快照归属、不加做梦锁
- **状态**：`[待实施]`
- **文件/段落**：`api/server.py` → `restore()`（L1354-1403）
- **怎么改**：
  1. 归属校验（load_state 前，用 `loop.snapshot.get_metadata(full_path)` 轻量读，避免整包 load）：
     ```python
     try:
         meta = loop.snapshot.get_metadata(full_path)
         owner = (meta or {}).get("session_id")
     except Exception:
         owner = None   # 读不到元数据 → 放行（fail-open，向后兼容旧快照）
     if owner and str(owner) != str(session_id):
         raise HTTPException(status_code=400, detail=f"快照归属会话 {owner}，与 {session_id} 不符，拒绝跨会话恢复")  # [B3]
     ```
  2. 做梦锁（load_state 是写路径）：
     ```python
     scheduler = get_dream_scheduler()
     scheduler.register_session(session_id)
     acquired = scheduler.acquire_conversation(session_id)
     if not acquired:
         raise HTTPException(status_code=503, detail="系统正在做梦，请稍后重试")
     try:
         loop.load_state(full_path)
         ...
     finally:
         scheduler.release_conversation(session_id)
     ```
- **注意事项**：路径钳制（_clamp_snapshot_path）保持不动；返回结构不变。
- **验证**：`tests/test_api_server.py` 新增断言：恢复他会话快照 → 400（允许新增断言）。

### [B4][3] `api/server.py:1301-1351` | /snapshot 不加做梦锁 → 梦期落盘半巩固状态
- **状态**：`[待实施]`
- **文件/段落**：`api/server.py` → `snapshot()`（L1301-1351）
- **怎么改**：在 `loop.save_session_state()` 前 acquire、finally release（与 B3 同款代码块；失败 503）。
- **注意事项**：审计字段（old sha256 等）保持；响应结构不变。
- **验证**：`tests/test_api_server.py` 新增断言：模拟做梦占用时 /snapshot 返回 503（允许新增断言）。

### [B5][3] `api/server.py:669/786/877/1171` | async 端点内同步阻塞锁冻结事件循环
- **状态**：`[待实施]`
- **文件/段落**：`api/server.py` → /chat（L669）、/feed（L786）、/store（L877）、/dream（L1171）的 `scheduler.acquire_conversation(...)`；`runtime/dream_scheduler.py::acquire_conversation()`（L194-214）
- **根因**：`acquire_conversation` 内部 `threading.Lock.acquire(timeout=10)` 是同步阻塞；async 端点直接调用 → 做梦期间整个事件循环冻结最长 10s、多会话写串行化。
- **怎么改**（最小侵入）：四处 acquire 改为线程池执行：
  ```python
  acquired = await asyncio.get_event_loop().run_in_executor(
      None, lambda: scheduler.acquire_conversation(req.session_id))   # [B5] 同步锁移出事件循环
  ```
  （release 不阻塞，留在 finally 原样。）
- **注意事项**：备选方案（每会话锁 + 全局做梦锁两级）重构面大，不推荐本轮实施；`scheduler.acquire_conversation` 本身不动（供非 async 调用方使用）。
- **验证**：`tests/test_dream_scheduler.py` / `tests/test_api_server.py` 跑既有用例 + 新增断言：并发请求期间事件循环仍可服务 /health（允许新增断言，可 mock acquire 延迟）。

### [B6][3] `api/session_manager.py:66-67` | 空 session_id 静默兜底 "default"
- **状态**：`[待实施]`
- **文件/段落**：`api/session_manager.py` → `get_or_create()`（L66-67）
- **怎么改**：
  ```python
  if not session_id:
      logger.warning("收到空 session_id，兜底为 'default'（客户端字段拼写错误会污染 default 脑）")  # [B6]
      session_id = "default"
  ```
- **注意事项**：行为不变，只加可见性；不改端点（端点层可选加 400，本轮不做）。
- **验证**：py_compile + 跑 `tests/test_api_server.py`。

### [B7][3] `core/doubt/gap_registry.py:218-238` | 消解/判重键与登记去重键不一致 → 已消解悬案可复活
- **状态**：`[待实施]` —— 与 **A4 同一根因**，由 A4 的统一判定助手 `_same_case` 一并覆盖，本条目不单独实施（实施清单打勾时以 A4 为准）。
- **验证**：同 A4。

### [B8][3] `api/control.py:507-557` | /control/config 假生效
- **状态**：`[待实施]`
- **文件/段落**：`api/control.py` → `control_config_update()`（L507-557）、`_effective_config()`/`_overrides` 读取（L492-496）
- **根因**：热配置只写 `_overrides`（控制面 :8191 进程内存），数据面 :8190 是**独立进程**直读 os.environ → 显示"已应用"实际零变化（跨进程 os.environ 写入也无济于事；数据面也无 reload-config 端点——审计标注的 T2.1 端点不存在）。
- **怎么改**（诚实标注 + 审计告警，不新增端点）：
  1. 响应体追加：`"data_plane_effect": "none"`，`"note"` 改为明确文案："控制面与数据面 :8190 为独立进程且数据面无 reload 端点：本更新仅记账展示，**对数据面零生效**，需重启数据面（或待 [需批准] 新增 reload 转发端点）"。
  2. `_audit(...)` 追加 `data_plane_applied: False`。
  3. 更新成功时 `logger.warning(...)` 显式告警假生效。
  - 注释：`# [B8] 热配置假生效修复：跨进程无法直写数据面 env；明示零生效+审计告警，不制造"已应用"假象`
- **注意事项**：备选方案（新增 `POST /control/reload-config` 转发端点让数据面重读 .env）是**新增端点 → [需批准]**，本轮只做诚实标注。
- **验证**：py_compile + 手测 /control/config 响应含 `data_plane_effect: "none"`。

### [B9][3] `api/control.py:604-659,200-213` | 注册下发的 client token 无消费方校验
- **状态**：`[待实施]`
- **文件/段落**：`api/control.py` → `_check_auth()`（L200-213）与 `control_register()`（L604-659）
- **根因**：注册流程下发一次性 token（存 sha256），但 `_check_auth` 只比对 CONTROL_TOKEN → "只发不收"半成品鉴权。
- **怎么改**（二选一，推荐 ①）：
  1. **校验注册表 token**：`_check_auth` 中，在 CONTROL_TOKEN 比对失败后追加：
     ```python
     if not hmac.compare_digest(supplied, CONTROL_TOKEN):
         if not _registered_token_valid(supplied):
             ... 401 ...
     ```
     其中 `_registered_token_valid(token)`：`sha256(token) in {r.get('token_hash') for r in _load_registrations()}`。
     - 注释：`# [B9] 消费方校验：注册下发的 client token（sha256 比对注册表）与 CONTROL_TOKEN 二选一通过`
  2. 备选：移除注册流程（改动大，影响已注册接入方，不推荐）。
- **注意事项**：`_load_registrations()` 每次调用读 access.jsonl（控制面低频，可接受；若性能顾虑可加 60s TTL 缓存）；审计记录"注册 token 通过"以区分来源。
- **验证**：手测：用 register 返回的 token 调 /control/access 成功、伪造 token 401。

### [B10][3] `tools/archive_job.py:139-169 + core/archive/archive_store.py:393-433` | 归档重建整体替换 → 永久丢失窗口外条目
- **状态**：`[待实施]`
- **文件/段落**：`core/archive/archive_store.py` → `rebuild_archive()`（L393-433）；`tools/archive_job.py` → `cmd_rebuild()`（L139-169）
- **根因**：rebuild 用扫描到的 records 整体重写归档文件（os.replace）→ 不在保留快照中的旧归档条目永久丢失（窗口外记忆被工具重新引入丢失）。
- **怎么改**：`rebuild_archive` 语义改为"**按 (turn, text_hash) 去重合并、只增不删**"：
  ```python
  # [B10] rebuild 改为合并语义：先读现有归档行（去重建 seen），再追加新记录，
  # 只增不删——窗口外旧归档条目不再被整体替换丢失
  existing = list(_iter_records(path)) if path.exists() else []
  seen = set()
  uniq = []
  for rec in existing:
      turn = int(rec.get("turn", -1) or -1)
      h = rec.get("text_hash") or text_hash(str(rec.get("text", "")))
      key = (turn, h)
      if key not in seen:
          seen.add(key); uniq.append(rec)
  for rec in records:
      ... 同上去重追加 ...
  # 然后照旧 tmp 原子写 + os.replace
  ```
  同步更新 docstring（"重建"→"合并去重，只增不删"）与 `cmd_rebuild` 的提示文案。
- **注意事项**：`_iter_records`（L436-439 用）已有坏行跳过逻辑；锁与原子替换保留。
- **验证**：跑 `tests/test_episodic_migration.py`（归档相关既有用例）；新增断言：重建后旧条目仍在（允许新增断言）。

### [B11][3] `api/server.py:1455-1472` | DELETE /sessions/{sid} 不删快照/归档 → 会话"复活"
- **状态**：`[待实施]`
- **文件/段落**：`api/server.py` → `delete_session()`（L1455-1472）
- **怎么改**：新增可选查询参数 `purge: bool = False`（追加参数，不改既有签名语义）：
  ```python
  @app.delete("/sessions/{session_id}")
  async def delete_session(session_id: str, purge: bool = False):
      ...
      if purge:
          # [B11] purge=true 同步删除快照目录与归档，杜绝"删除后访问即复活"
          import shutil
          sdir = loop._snapshot_dir_path() if False else None  # 不引 loop（已删）
          snap_root = get_snapshot_dir()  # 需按 config['snapshot_dir'] 口径；实现时复用 loop._snapshot_dir_path()（在 remove 前取得）
          sid_safe = sanitize_session_id(session_id)
          shutil.rmtree(os.path.join(snap_root, sid_safe), ignore_errors=True)
          arch = os.path.join(str(getattr(core.paths, ...)), ...)  # data/archive/{sid}.jsonl（实现时用 archive_path_for(sid)）
          for p in [arch]:
              if os.path.isfile(p): os.remove(p)
      ...
  ```
  实现要点：`remove` 之前先取 `snap_dir = sm.get(session_id)._snapshot_dir_path()`；归档路径用 `core/archive/archive_store.archive_path_for(sid)`（需查该函数签名）。
- **注意事项**：默认 `purge=False` 保持现状（向后兼容）；purge=true 属破坏性操作，响应注明已删路径清单；审计补 purge 标记。
- **验证**：手测：purge=true 后 snapshots/{sid}/ 与归档文件消失。

### [B12][3] `api/config.py:187-188/223-224/250-271` | env 空串时 int()/float() 崩溃 500
- **状态**：`[待实施]`
- **文件/段落**：`api/config.py` → `_get_env()`（L65-68）与全部 `int(...)`/`float(...)` 调用点（L95、L187-188、L223-224、L250-271、L285）
- **根因**：`_get_env` 对空串直接返回 `""` → `int("")`/`float("")` 抛 ValueError → 配置构建崩溃，且报错不指向具体键。
- **怎么改**：新增安全解析助手并替换全部调用点：
  ```python
  def _env_int(key: str, default: int) -> int:
      """[B12] 安全 int 解析：空串/非法值回退默认并 WARNING 指明键名（旧实现直接崩溃 500）。"""
      try:
          return int(_get_env(key, str(default)))
      except (TypeError, ValueError):
          logger.warning("环境变量 %s 非法/为空（%r），回退默认 %s", key, os.environ.get(key), default)
          return default

  def _env_float(key: str, default: float) -> float:
      try:
          return float(_get_env(key, str(default)))
      except (TypeError, ValueError):
          logger.warning("环境变量 %s 非法/为空（%r），回退默认 %s", key, os.environ.get(key), default)
          return default
  ```
  替换点：L95 `int(_get_env("LMS_CLOUD_EMBED_DIM","1024"))` → `_env_int("LMS_CLOUD_EMBED_DIM", 1024)`；L187-188 → `_env_int("LMS_NUM_NODES", config['num_nodes'])` 等；L223-224、L250-271 全部 j_allostatic_* → `_env_int/_env_float`；L285 → `_env_int("LMS_API_PORT", DEFAULT_API_PORT)`。
- **注意事项**：**.env 红线值不变**——本修复只是"空串/非法时不崩、回退默认并告警"，不改任何默认值（LMS_J_TARGET_NORM 默认 40.0 保持，.env 里的 12 照常生效）。
- **验证**：`tests/test_config.py` 新增断言：空串 env → 返回默认且不抛异常（允许新增断言）。

### [B13][2] `api/wiring.py` | M1 接线模块是死代码，双实现漂移
- **状态**：`[待实施]`
- **文件/段落**：`api/wiring.py`（整文件，533 行）
- **根因**：无任何生产 import（全仓库 grep 无引用），与 server.py 内联 writer 双实现漂移 → 若未来切到 wiring 会静默丢字段。
- **怎么改**（推荐：删除）：
  - `git rm api/wiring.py`；其设计决策（D-1~D-6）与 claim 登记已在本计划与本文件存档（见下备注），不丢失。
  - 若 dandan 顾虑删除动作：退而求其次在文件头加 `DeprecationWarning` 注释并注明"死代码，勿在生产 import；/store /feed 语义唯一权威在 api/server.py + core/store"。
- **注意事项**：删除前确认无 import（已 grep 证实：无）；`tests/` 无 test_wiring_m1.py，无测试引用。
- **验证**：删除后 `python -c "import api.wiring"` 报 ImportError（预期）；跑 `tests/test_store_endpoint.py`（server.py 路径不受影响）。

### [B14][2] `api/server.py:927-934` | /store meta 用 entries[-1] 假设"本轮只追加一条"
- **状态**：`[待实施]`
- **文件/段落**：`api/server.py` → `store()` 的 writer 回调（L926-939）
- **根因**：`new_entry = entries[-1]`（L928）只给最后一条打 meta；process_turn 实际可能追加 0~N 条。
- **怎么改**：改用 `epi_before`（L884 已有）切片：
  ```python
  entries = list(loop.memory.iter_episodic())
  new_entries = entries[epi_before:] if loop.memory.episodic_size() > epi_before else []   # [B14] 本轮实际新增条目（可多条），逐条打 meta
  for new_entry in new_entries:
      new_entry.core = core or None
      new_entry.ts = time.time()
      if gray:
          new_entry.gray = True
          ...
  ```
- **注意事项**：原逻辑的 `episodic_size() > epi_before` 判定保留；info_value/reason 等后续段需逐条处理（实施时通读 L926-970 整段，把单条目假设改成循环）。
- **验证**：`tests/test_store_endpoint.py` 跑既有用例；新增断言：单轮多条入库时每条 meta.core 均被设置（允许新增断言）。

### [B15][2] `api/server.py:152-160,231-238` | SimpleChatRequest/FeedRequest 缺 sid 兼容别名
- **状态**：`[待实施]`
- **文件/段落**：`api/server.py` → `SimpleChatRequest`（L152-154）、`FeedRequest`（L231-238）
- **怎么改**：照抄 StoreRequest（L265-281）/RecallRequest（L209-225）同款模式：
  - 两个模型各加 `sid: Optional[str] = Field(None, ...)` + `@model_validator(mode="after") def _apply_sid_alias`；SimpleChatRequest 默认 session "default"、FeedRequest 默认 "bus"，alias 映射当 `self.session_id == 默认值` 时采用 sid 并 WARNING（与既有实现逐字同款）。
  - 注释：`# [B15] 补 sid 兼容别名：旧客户端 sid 被静默丢弃 → 与其余请求模型一致`
- **注意事项**：响应模型不动；pydantic v2 model_validator 已在本文件多处使用，照抄即可。
- **验证**：`tests/test_api_server.py` 新增断言：/chat/simple 传 sid → session_id 生效（允许新增断言）。

### [B16][2] `bridge/translation_layer.py:561-564` | recall_from_hourglass 的 embed 绕过熔断器
- **状态**：`[待实施]`
- **文件/段落**：`bridge/translation_layer.py` → `recall_from_hourglass()`（L561-564）
- **怎么改**：与 `bridge/encoder.py:67` 同款，走熔断器：
  ```python
  from core.sensory.circuit_breaker import get_default_embed_circuit
  cb = get_default_embed_circuit()
  if hasattr(embedder, 'embed_text'):
      query_vector = cb.call(lambda: embedder.embed_text(query_text))      # [B16] 统一走熔断器，故障不裸抛/挂起
  elif hasattr(embedder, 'embed_text_raw'):
      query_vector = cb.call(lambda: embedder.embed_text_raw(query_text))
  ```
  （实现时先确认 `EmbedCircuitBreaker.call` 的入参形态与 encoder.py:67 完全一致再照抄。）
- **注意事项**：hourglass_client 缺失的 RuntimeError / embedder 能力检查（L566-569）保持不动。
- **验证**：`tests/test_translation_layer.py` 跑既有用例；新增断言：熔断打开时 recall_from_hourglass 抛 CircuitOpenError 而非裸抛 embed 异常（允许新增断言）。

### [B17][2] `core/doubt/gap_registry.py` | 无锁，跨线程读写竞态
- **状态**：`[待实施]`
- **文件/段落**：`core/doubt/gap_registry.py`（全文件）
- **怎么改**：
  - `__init__` 加 `self._lock = threading.RLock()`（RLock：mark_resolved→_persist 等嵌套调用安全）。
  - 在 `register_*`、`mark_resolved`、`is_resolved`、`resolved_list`、`resolved_count`、`mark_review`、`snapshot`、`doubt_lamp`、`review_stats`、`_persist`、`_load` 各方法体包 `with self._lock:`（读方法也持锁，防 json.dump 时列表被改 → RuntimeError；列表推导期间增删丢更新）。
  - 注释：`# [B17] 全方法持 RLock：做梦线程 vs API 线程跨线程读写竞态 → json.dump RuntimeError/丢更新`
- **注意事项**：RLock 可重入，嵌套调用无死锁；性能开销可忽略（低频登记/读出）。
- **验证**：`tests/test_doubt_m3.py` / `tests/test_doubt_integration.py` 跑既有用例 + 新增并发断言：多线程并发 register+snapshot 不抛异常（允许新增断言）。

### [B18][2] `tools/clean_episodic.py:129-132` | 无锁直写快照，服务运行中清洗不生效
- **状态**：`[待实施]`
- **文件/段落**：`tools/clean_episodic.py` → `main()`（L129-135 写回段，入口在 L85 附近）
- **怎么改**：入口处 fail-closed 探测：
  ```python
  # [B18] 服务运行中清洗不生效（内存每 34s 覆盖磁盘）：探测 :8190 存活即拒绝
  import urllib.request
  try:
      with urllib.request.urlopen("http://127.0.0.1:8190/health", timeout=5) as r:
          if r.status == 200:
              print("❌ LMS :8190 正在运行：清洗会被内存覆盖且无锁直写有撕裂风险，请先停服（或加 --force 显式覆盖）")
              return 1
  except Exception:
      pass  # 不可达 → 允许继续
  ```
- **注意事项**：备选方案（走 Snapshot.save 持锁）改动大；本轮用 fail-closed 拒绝，简单安全。
- **验证**：服务运行时手测 exit 1 + 提示。

### [B19][2] `api/session_manager.py:69-88` | get_or_create 持全局锁执行 torch.load 自动恢复 → 阻塞所有会话
- **状态**：`[待实施]`
- **文件/段落**：`api/session_manager.py` → `get_or_create()`（L69-89）
- **怎么改**（会话级锁方案）：`_try_auto_restore`（torch.load，可能秒级）移出全局 `_lock`：
  - `__init__` 加 `self._session_locks: Dict[str, threading.Lock] = {}`。
  - `get_or_create` 重构：
    ```python
    if not session_id: ...（B6）
    with self._lock:
        existing = self._sessions.get(session_id)
    if existing is not None:
        return existing
    with self._session_lock(session_id):          # 会话级锁（B19：首访的 torch.load 只阻塞同 sid）
        with self._lock:
            loop = self._sessions.get(session_id)
            if loop is not None:
                return loop
            cfg = dict(config) if config else self._default_config_factory()
            cfg['session_id'] = session_id
            loop = LivingMemoryLoop(cfg)
            self._sessions[session_id] = loop
            self._configs[session_id] = cfg
            audit("session_created", ...)
        self._try_auto_restore(loop, session_id)   # 全局锁外执行
    return loop
    ```
  - `_session_lock` 助手：在全局锁内 get-or-create 会话锁。
- **注意事项**：`get()`/`remove()`/`list_sessions()` 保持全局锁（轻量）；并发首访同 sid 由会话锁串行。
- **验证**：`tests/test_api_server.py` 跑既有用例 + 新增断言：两个 sid 并发 get_or_create 时第二个不被阻塞（允许新增断言，可 mock torch.load 延迟）。

### [B20][2] `bridge/mcp_server.py:211-217` | save_snapshot 接受客户端任意路径写文件
- **状态**：`[待实施]`
- **文件/段落**：`bridge/mcp_server.py` → `_cmd_save_snapshot()`（L206-218）
- **怎么改**：路径钳制到 snapshots/ 内：
  ```python
  from persistence.snapshot import sanitize_session_id, snapshot_path_for, latest_path_for
  # [B20] 客户端路径钳制：仅允许 snapshots/ 内（复用持久层命名，防任意路径写文件）
  snap_root = Path(__file__).resolve().parent.parent / "snapshots"
  raw = params.get("path") or ""
  if raw:
      cand = Path(raw).expanduser()
      try:
          cand = cand.resolve()
          root = snap_root.resolve()
          cand.relative_to(root)          # 非 snapshots/ 内 → ValueError
      except (ValueError, OSError):
          return {"error": f"非法快照路径（仅允许 {snap_root}/ 内）: {raw}"}
      path = str(cand)
  else:
      path = str(snap_root / sanitize_session_id(getattr(self.loop, 'session_id', 'main')) /
                 f"snapshot_mcp_{int(time.time())}.pt")
  ```
- **注意事项**：`api/server.py::_clamp_snapshot_path`（L579）在 server 模块（import fastapi 重），mcp_server 不复用，改为本地同语义钳制；目录创建保留。
- **验证**：手测：传 `../../etc/passwd` → error；不传 path → 生成 snapshots/ 内快照。

### [B21][1] `api/server.py:357-419,490-492` | 旧幂等/限流死代码残留
- **状态**：`[待实施]`
- **文件/段落**：`api/server.py` → `_store_dedup_key()`（L400-403）、`_store_dedup_hit()`（L406-419）、`_feed_rate_limited()`（L490-492）
- **根因**：无调用点（/store 幂等已由 core/store/ingest 承接，/feed 直接用 `_feed_rate_check`）→ 误导维护者以为仍在生效。
- **怎么改**：删除三段死代码，原地留注释指向新实现：
  ```python
  # [B21] 旧幂等去重死代码已删除：/store 幂等唯一权威 = core/store/ingest（60s 窗口）
  # [B21] 旧 _feed_rate_limited 包装已删除：/feed 直接用 _feed_rate_check（L773）
  ```
- **注意事项**：`_store_rate_check`（L422，被 L853 使用）与 `_feed_rate_check`（L472，被 L773 使用）**保留**；`store_503_count`/`_store_count_503` 保留（被 /store 使用）。
- **验证**：py_compile + 跑 `tests/test_store_endpoint.py`（限流/503 语义不变）。

### [B22][1] `bridge/llm_bridge.py:169` | LLM 返回 content=None 被误判为失败
- **状态**：`[待实施]`
- **文件/段落**：`bridge/llm_bridge.py` → `query()`（L169）；`api/server.py` → /chat（L690 `len(response_text)`）
- **怎么改**：
  - llm_bridge.py：
    ```python
    content = response.choices[0].message.content
    return (content or "")   # [B22] content=None 归一化为空串：None 会被误判调用失败（server.py len(None) TypeError）
    ```
  - server.py:690 防御：`f"[{req.session_id}] LLM 回复长度: {len(response_text or '')}"`。
- **注意事项**：归一化后 /chat 的 degraded 分支（L691-700）只对真异常触发；不改响应结构。
- **验证**：`tests/test_bridge.py` 新增断言：mock content=None → 返回 ""（允许新增断言）。

### [B23][1] `api/control.py:696-780` | /control/diagnose 只读探针有建会话副作用
- **状态**：`[待实施]`
- **文件/段落**：`api/control.py` → `control_diagnose()`（L696-780，探针 L704-710）
- **根因**：`POST /recall`（L705-708）内部 get_or_create("main") → 只读诊断制造幻影会话。
- **怎么改**：把 recall 探针替换为纯只读双探针：
  ```python
  # 1) 数据面存活 + 只读探针（/health + /landscape/main 均不创建会话——B23）
  api_status, api_body = await _ahttp_json("GET", "/health", timeout=3.0)
  ls_t0 = time.time()
  ls_status, ls_body = await _ahttp_json("GET", "/landscape/main", timeout=6.0)
  ls_ms = round((time.time() - ls_t0) * 1000, 1)
  ...
  "api": {
      "reachable": api_status == 200,
      "health": api_body.get("status") if isinstance(api_body, dict) else None,
      "landscape_probe": {"status": ls_status, "duration_ms": ls_ms, "ok": ls_status == 200},
  },
  ```
  （/landscape/{sid} 纯只读：sm.get 而非 get_or_create，L1218 实证。）
- **注意事项**：响应新增 `landscape_probe` 字段（追加字段，向后兼容）；embed 探针（L720-746）保留（直连 embed URL，无会话副作用）。
- **验证**：手测：诊断前 `/sessions` 无 main → 诊断后仍无 main（不新建）。

---

## 4. 模块 C：运维脚本（scripts/ + *.sh + tools/ 迁移工具）

### [C1][5] `lms_backup.sh:162-180,217-235` | hourly/daily tar 连续多日失败（file changed）
- **状态**：`[待实施]`（P0，备份止血，第一优先）
- **文件/段落**：`scripts/lms_backup.sh` → `run_hourly()`（L162-169）与 `run_daily()`（L217-224）
- **根因**：live 每 ~34s 写快照 → tar 读时文件变化 → GNU tar 返回码 1（--warning=no-file-changed 只抑制显示不改变退出码）→ `if !(tar)` 判失败 → `rm -f $tmp_archive` → hourly/daily 归档连续失败，灾备只剩 15min rsync 一层。
- **怎么改**（方案①：rc + stderr 识别；另附方案②备选）：
  ```bash
  # [C1] tar 因"读取时文件变化"返回 1 属常态（live 每 34s 写快照），
  # 不能当失败丢弃归档：捕获 stderr 判别后视为成功
  err_file="$HOURLY_DIR/.lms-hourly-$stamp.err.$$"
  if ! ( cd "$LMS_HOME" && tar --zstd -cf "$tmp_archive" \
          --exclude='*.lock' --exclude='*.tmp' --exclude='*.swp' \
          snapshots 2>"$err_file" ); then
      rc=$?
      if [ "$rc" -eq 1 ] && grep -q 'file changed as we read it' "$err_file" 2>/dev/null; then
          log "tar 因快照持续写入返回 1（file changed），归档内容仍完整，按成功处理"
      else
          rm -f "$tmp_archive" 2>/dev/null || true
          rm -f "$err_file" 2>/dev/null || true
          fail "每小时归档 tar.zst 打包失败（rc=$rc）"
          return 1
      fi
  fi
  rm -f "$err_file" 2>/dev/null || true
  ```
  run_daily 同款替换（L217-224）。
- **注意事项**：方案②（先 rsync 到暂存再打包）作为备选；cron 错开整点竞争是运维侧动作（不在仓库内）；zstd -t / tar -tf 冒烟校验（L172-180）保留。
- **验证**：手测：live 运行中跑一次 run_hourly → 归档生成且 manifest 记录；`zstd -t` 通过。

### [C2][4] `lms_entropy_watchdog.py:126-129,199-206` | 换蛋恢复 latest_main.pt（近当前态）而非真健康蛋
- **状态**：`[待实施]`
- **文件/段落**：`scripts/lms_entropy_watchdog.py` → 健康快照记录（L199-206）、`dispose_burnt()`（L123-129）
- **根因**：last_healthy 只记录最新 mtime 快照（每 34s 覆盖的近当前态）→ 恢复后 ~35 分钟再度饱和 → 换蛋循环，被回滚记忆永久丢失。
- **怎么改**（按 lms-field-restore 技能选蛋标准）：
  1. 新增选蛋函数：
     ```python
     EGG_MIN_AGE_S = 2 * 3600        # [C2] 蛋龄下限 2h（近当前态不是蛋，是刚孵化的鸡）
     EGG_J_BAND = (5.0, 9.0)         # [C2] J 范数健康带（健康≈7；烧焦≈12）
     EGG_ISOLATION_S = 2 * 3600      # [C2] 换蛋后隔离期：2h 内不记录 last_healthy
     GRAY_FIX_MTIME = 1724322532.0   # [C2] 灰池修复完成时刻（2026-08-22 12:28:52），早于它的蛋不用

     def _probe_snapshot_jnorm(snap_path):
         """[C2] 子进程 torch.load 探测 J 范数（fail-open 返回 None）。"""
         script = ('import torch,warnings; warnings.filterwarnings("ignore");'
                   f'd=torch.load({os.path.join(SNAP_DIR, snap_path)!r},'
                   ' map_location="cpu", weights_only=False);'
                   'J=d["attractor"]["J"];'
                   'print(round(float(torch.norm(J, p="fro").item()),2))')
         try:
             proc = subprocess.run([sys.executable, "-c", script], cwd="...",
                                   env=..., capture_output=True, text=True, timeout=60)
             return float(proc.stdout.strip().splitlines()[-1])
         except Exception:
             return None

     def _pick_healthy_egg(now):
         """[C2] 候选蛋筛选：蛋龄≥2h + mtime≥灰池修复 + J∈[5,9] + 未烧。"""
         best = None
         for p in sorted(os.listdir(SNAP_DIR), reverse=True):
             if not p.endswith(".pt"): continue
             full = os.path.join(SNAP_DIR, p)
             age = now - os.path.getmtime(full)
             if age < EGG_MIN_AGE_S or os.path.getmtime(full) < GRAY_FIX_MTIME:
                 continue
             j = _probe_snapshot_jnorm(p)
             if j is None: continue
             if EGG_J_BAND[0] <= j <= EGG_J_BAND[1]:
                 return p
         return best
     ```
  2. 健康分支（L199-206）改为：`state["last_healthy"] = _pick_healthy_egg(now)`；换蛋后隔离期内（`now - last_dispose < EGG_ISOLATION_S`）跳过记录。
  3. `dispose_burnt` 恢复前校验 `last_healthy` 满足蛋龄（旧毒蛋字段直接弃用）。
- **注意事项**：探测子进程每次候选最多 1 次 torch.load（按 mtime 从新到旧，第一个合格即返回，通常 1 次）；`latest_snapshot()`（L73-81）不再用于选蛋（可保留给日志）。
- **验证**：构造带 fake .pt 的临时 SNAP_DIR 跑 `_pick_healthy_egg` 单测（新增断言允许）。

### [C3][4] `lms_entropy_watchdog.py:84-120` | σ 重置对 live 进程无效 + 两进程并发写撕裂 —— **`[需批准]`（新增端点，血管级）**
- **状态**：`[需批准]` —— **设计进方案，待 dandan 批准后实施**；获批前 watchdog 侧只做安全止血（见下）。
- **根因**：独立进程 get_or_create 加载磁盘副本 reset + save；live :8190 内存 σ 依然饱和；live 每 34s 覆盖磁盘 → 重置仅存活 ~34s；两进程并发写同一 latest_main.pt 有撕裂风险。
- **设计方案（获批后实施）**：
  1. **数据面新增端点**（api/server.py，新增 → 血管级）：
     ```python
     @app.post("/reset-sigma/{session_id}")
     async def reset_sigma(session_id: str):
         """[C3][需批准] 进程内 σ 重置（保留 J/记忆）：live 脑 sigma 归零并立即落盘。
         替代 watchdog 独立进程磁盘副本重置（对 live 无效且并发写撕裂）。"""
         sm = get_session_manager(); loop = sm.get(session_id)
         if loop is None:
             raise HTTPException(status_code=404, detail=f"会话 '{session_id}' 不存在")
         scheduler = get_dream_scheduler()
         scheduler.register_session(session_id)
         acquired = scheduler.acquire_conversation(session_id)
         if not acquired:
             raise HTTPException(status_code=503, detail="系统正在做梦，请稍后重试")
         try:
             loop.attractor.reset_state()
             path = loop.save_session_state()
             return {"session_id": session_id, "sigma_reset": True,
                     "path": path, "j_preserved": True}
         finally:
             scheduler.release_conversation(session_id)
     ```
  2. **控制面转发端点**（api/control.py，新增）：`POST /control/reset-sigma`（`_require_write` 鉴权）→ `_ahttp_json("POST", f"/reset-sigma/{quote(sid)}", ...)` 转发 :8190，超时钳制 + fail-open。
  3. **watchdog 改写** `reset_sigma()`：改为 `urllib` 调控制面/数据面端点（带 token），不再 spawn 子进程读写磁盘。
- **获批前的安全止血（本轮可实施，不新增端点）**：`reset_sigma()` 改为**不写磁盘、只告警**：
  ```python
  def reset_sigma(state, reason):
      """[C3] 获批前止血：不写磁盘（避免与 live 每 34s 快照并发写撕裂）。
      仅 CRIT 告警，提示走 /control/reset-sigma（待 dandan 批准后接线）。"""
      log(f"♻️ σ 饱和（{reason}）——进程内重置端点未获批，本轮不处置、不写盘；建议人工 /control/reset-sigma")
      return False
  ```
- **注意事项**：新增端点不改变任何现有签名；端点路径 /reset-sigma/{sid} 与 /control/reset-sigma 均为新增；`save_session_state` 持写锁（Snapshot.save fcntl），进程内安全。
- **验证（获批后）**：手测 POST /reset-sigma/main → /landscape/main σ max 归零且 /status 正常；watchdog 模拟饱和 → 日志显示端点调用成功。

### [C4][4] `lms_entropy_watchdog.py:199-206` | 健康分支吞掉 σ 饱和 onset + 记录毒蛋
- **状态**：`[待实施]`
- **文件/段落**：`scripts/lms_entropy_watchdog.py` → `main()`（L196-236）
- **根因**：`if sigma_max < 0.9 or entropy < ENTROPY_HEALTHY`（L199）先于烧焦判定 → σ 饱和但熵<0.995 的 onset 阶段把饱和快照记为 last_healthy（毒蛋）且 return，σ 重置分支永不触发。
- **怎么改**：重构判定顺序——**先判烧焦/饱和，再判健康**：
  ```python
  sigma_sat = sigma_max > SIGMA_SATURATED
  ent_burnt = entropy > ENTROPY_BURNT

  if ent_burnt:
      # 烧焦：streak/换蛋（原 L225-233 逻辑）
      ...
  elif sigma_sat:
      # σ 饱和但熵未烧焦：σ 重置分支（原 L214-223 逻辑；冷却照旧）
      ...
  else:
      # [C4] 健康记录：σmax<0.9 **且** 熵<ENTROPY_HEALTHY 同时成立才记录；
      # σmax>0.9 时绝不写 last_healthy（防毒蛋）；并叠加 C2 的选蛋/隔离期
      if sigma_max < 0.9 and entropy < ENTROPY_HEALTHY:
          snap = _pick_healthy_egg(now)
          if snap:
              state["last_healthy"] = snap
              state["streak"] = 0
              write_state(state)
      else:
          state["streak"] = 0
          write_state(state)
  ```
- **注意事项**：ENTROPY_HEALTHY=0.995 < ENTROPY_BURNT=0.9995 之间存在中间带（0.995≤熵≤0.9995 且 σ<0.9）：不健康也不烧焦 → 只清 streak，不记录健康蛋（保守正确）。
- **验证**：模拟三态（烧焦/饱和/健康）跑 main() 逻辑单测（新增断言允许）。

### [C5][4] `lms_entropy_watchdog.py:158-174` | dispose_burnt 不检查 returncode、重启后不验证
- **状态**：`[待实施]`
- **文件/段落**：`scripts/lms_entropy_watchdog.py` → `dispose_burnt()`（L123-174）
- **怎么改**：
  1. 恢复脚本 returncode 检查：
     ```python
     if proc.returncode != 0:
         log(f"❌ 恢复脚本失败（rc={proc.returncode}），不重启，交人工: {proc.stderr.strip()[-200:]}")
         return False   # [C5] 假成功换蛋 → 30 分钟盲区；rc!=0 一律不重启
     ```
  2. 重启后验证（兑现 docstring"等 20s 验证"承诺）：
     ```python
     time.sleep(20)
     st2 = http_get("/status/main"); ls2 = http_get("/landscape/main")
     ent2 = float(st2.get("status", {}).get("entropy_ratio", 0) or 0)
     top2 = ls2.get("landscape", {}).get("activation", {}).get("top_activated", [])
     sig2 = max([n.get("sigma", 0) for n in top2], default=0)
     if ent2 > ENTROPY_BURNT or sig2 > SIGMA_SATURATED:
         log("❌ 重启后仍烧焦/饱和——不置 last_dispose（下轮重试/升级人工）")
         return False
     log("✅ 换蛋成功且验证通过（熵 %.4f / σmax %.3f）" % (ent2, sig2))
     return True
     ```
- **注意事项**：`main()` 中 `if dispose_burnt(...): state["last_dispose"] = now` 逻辑不变（False 时不置冷却 → 下轮可重试）。
- **验证**：模拟恢复脚本 rc=1 → 不重启；模拟重启后仍烧焦 → 返回 False。

### [C6][3] `lms_ops_monitor.py:157,345-348` | 孤儿进程误报风暴
- **状态**：`[待实施]`
- **文件/段落**：`scripts/lms_ops_monitor.py` → `classify_processes()`（L140-159）、L3 检查（L345-348）
- **根因**：api.run / run_control 是 setsid 守护（PPID=1 是设计常态）→ 每 5 分钟 WARN 一条，告警通道被噪音淹没。
- **怎么改**：孤儿判定排除 API 主进程与控制面，只对 MCP/glue 判孤儿：
  ```python
  # [C6] api.run/run_control 是 setsid 守护，PPID=1 属设计常态（crontab 注释实锤），
  # 不判孤儿（API 可用性由 L1 /health 覆盖）；孤儿只针对 MCP/glue 回收对象
  orphans = [p for p in mcp + http_mcp + glue if p["ppid"] == 1]
  ```
  L345 告警文案同步改为"MCP/glue 孤儿"。`_PROC_PATTERNS`（control.py 侧同款）不动。
- **注意事项**：若未来 api.run 真成孤儿且不可服务，L1 CRIT 已覆盖；不改 L1。
- **验证**：跑 `scripts/lms_ops_monitor.py --level L3` 一次，确认无孤儿误报（在有 setsid API 进程时）。

### [C7][3] `lms_dsh_feed.py:156-161` | 水位跳到未配对 user 消息的 seq
- **状态**：`[待实施]`
- **文件/段落**：`scripts/lms_dsh_feed.py` → `main()`（L155-161，配对逻辑 L112-153）
- **根因**：`posted==0` 时水位直接推进到 max_seq → 未配对 user 消息（及其后续 assistant 回复）永久不喂入 LMS。
- **怎么改**：水位只推进到**最后一个已配对 assistant seq**：
  ```python
  last_paired_seq = prev_seq   # [C7] 只记录成功 POST 的 assistant seq
  ...
  # 成功 POST 后：last_paired_seq = seq（在 L145 水位写处同步更新）
  ...
  # 收尾（L155-161）：
  if max_seq > prev_seq and posted == 0:
      watermark = {"file": path, "seq": last_paired_seq, "backfilled": backfilled}
      ...   # [C7] 不跳未配对 user seq：水位停在最后已配对 assistant seq，
            # 未配对 user 下轮续配（seq 未推进 → 下轮重新扫描该事件）
      print(...)
  ```
- **注意事项**：`pending_user` 是本轮内存态，水位不越过未配对 user seq 即实现"下轮续配"（无需持久化 pending 字段）；已配对轮次的水位推进逻辑（L144-148）不变。
- **验证**：构造"user 无 assistant 回复"的事件流 → 水位不越过该 user seq；下一轮补上 assistant 后配对成功。

### [C8][3] `lms_dsh_feed.py:134-146` | 首次回填上限自破
- **状态**：`[待实施]`
- **文件/段落**：`scripts/lms_dsh_feed.py` → `main()`（L134-148）
- **根因**：达上限后 `backfilled=True`（L146）反而解除上限（L134 `if not backfilled`）→ 同轮全量冲刷。
- **怎么改**：达上限时**推进水位但不置 backfilled**：
  ```python
  if not backfilled and turns >= BACKFILL_MAX_TURNS:
      # [C8] 达上限：跳过 POST，但推进水位（防无限重扫），保持 backfilled=False
      # → 下一轮继续按上限回填（上限是"每轮最多 N 轮"，不是"全局只回填 N 轮"）
      watermark = {"file": path, "seq": seq, "backfilled": False}
      with open(WATERMARK_FILE, "w") as f:
          json.dump(watermark, f)
      pending_user = None
      continue
  ```
  （L146 的水位写入在正常 POST 路径保持 `backfilled or turns >= BACKFILL_MAX_TURNS` 改为 `backfilled`——即只有完整扫完才置 True。）
- **注意事项**：watermark 每跳过一对就写一次文件（本轮内多次写可接受；或仅在跳过时更新内存、收尾统一写——实施时选简单方案）。
- **验证**：构造 >20 轮 backlog → 首轮只喂 20 轮且水位推进、backfilled=False；第二轮继续喂剩余。

### [C9][3] `lms_feed_docs_chunked.py:24-36,57-60` | 超长块被截断标记完成 + 失败 exit 0
- **状态**：`[待实施]`
- **文件/段落**：`scripts/lms_feed_docs_chunked.py` → `split_chunks()`（L24-36）、`main()`（L55-80）
- **怎么改**：
  1. `split_chunks` 加硬长度二次切分：
     ```python
     def split_chunks(text, size=CHUNK):
         """按句子边界分块；无边界超长段按硬长度二次切分（防文档尾部永久丢失）。"""
         parts = re.split(r"(?<=[。！？!?\n])", text)
         chunks, cur = [], ""
         for p in parts:
             while len(p) > size:            # [C9] 单段超长（无句边界）→ 硬切
                 if cur: chunks.append(cur.strip()); cur = ""
                 chunks.append(p[:size].strip()); p = p[size:]
             if len(cur) + len(p) > size and cur:
                 chunks.append(cur.strip()); cur = p
             else:
                 cur += p
         if cur.strip(): chunks.append(cur.strip())
         return [c for c in chunks if c]
     ```
     L60 的 `chunk[:CHUNK]` 保留为防御（二次切分后应恒 ≤CHUNK）。
  2. `main()` 失败计数返回非零：
     ```python
     failed = 0
     ...
     except Exception as e:
         print(f"FAIL ...", flush=True); failed += 1; time.sleep(2)
     ...
     print(f"done: fed={ok} total_chunks={total} failed={failed}", flush=True)
     return 1 if failed else 0   # [C9] 失败必须非零退出，运维可见
     ```
- **注意事项**：STATE 幂等记录不变；失败块不写 done → 下轮重试。
- **验证**：构造无句点长文本 → 所有字符均落入某 chunk；模拟失败 → exit 1。

### [C10][2] `lms_http_mcp.py:396-408` | stdin 多行 JSON 解析失败清空 buffer
- **状态**：`[待实施]`
- **文件/段落**：`lms_http_mcp.py` → `main()`（L396-408）
- **怎么改**：
  ```python
  buffer = ""
  for line in sys.stdin:
      buffer += line
      if buffer.strip():
          try:
              request = json.loads(buffer)
              response = handle_request(request)
              if response:
                  print(json.dumps(response, ensure_ascii=False))
                  sys.stdout.flush()
              buffer = ""            # [C10] 成功解析才清 buffer
          except json.JSONDecodeError:
              pass                    # 半截请求：保留 buffer，等下一行补全
  ```
- **注意事项**：持续无法解析的 buffer 会无限增长——上限保护（如 len(buffer) > 1MB 时清空+告警）可选加。
- **验证**：stdin 分段喂入一个 JSON（两行）→ 处理成功一次。

### [C11][2] `lms_entropy_watchdog.py:39-46 + crontab` | 双重日志 + 冷却等于 cron 周期
- **状态**：`[待实施]`（脚本内部分可改；crontab 行属运维侧，附操作说明）
- **文件/段落**：`scripts/lms_entropy_watchdog.py` → `log()`（L39-46）、`SIGMA_RESET_COOLDOWN_S`（L36）
- **怎么改**：
  1. `log()` 去掉 `print(line)`（只写文件）：cron 已把 stdout 重定向到同一日志文件 → 每条两行。
     ```python
     def log(msg):
         line = f"{time.strftime('%Y-%m-%d %H:%M:%S')} {msg}"
         try:
             with open(LOG_FILE, "a", encoding="utf-8") as f:
                 f.write(line + "\n")
         except Exception:
             pass
         # [C11] 不再 print：cron 重定向 stdout 到同一文件会造成双行日志；
         # 如需 stdout 留痕，改由 crontab 重定向到 /dev/null（运维侧）
     ```
  2. 冷却取周期 2 倍以上：`SIGMA_RESET_COOLDOWN_S = 600`（cron 周期 300s → 实际无冷却；600 = 2×周期），注释注明。
  3. 可选：记录连续重置次数，超过阈值升级为 CRIT 人工（state 增加 `sigma_reset_count`）。
- **注意事项**：crontab 行 `>> logs/lms_entropy_watchdog.log 2>&1` 改 `> /dev/null 2>&1` 是运维侧动作（crontab 不在仓库内），在本计划实施清单注明由 dandan/运维执行。
- **验证**：跑两次 watchdog（间隔 < 600s）→ 第二次跳过 σ 重置；日志无双行。

### [C12][2] `lms_ops_monitor.py:259-260,373` | L2 探针制造 default 幻影会话 + 字段名不副实
- **状态**：`[待实施]`
- **文件/段落**：`scripts/lms_ops_monitor.py` → `check_l2()`（L257-260）、`check_l3()`（L371-374、L244）
- **怎么改**：
  1. L2 探针改用**已存在会话**（先 GET /sessions 选一个真实会话；无会话则跳过 recall 探针，只报 health）：
     ```python
     # [C12] 探针不再写死 session_id="default"（会 get_or_create 幻影会话）：
     # 先取真实会话列表，无会话则跳过 recall 探针
     s_status, s_body = http_request(f"{self.base_url}/sessions", timeout=5)
     sids = (s_body or {}).get("sessions", []) if isinstance(s_body, dict) else []
     probe_sid = sids[0] if sids else None
     if probe_sid:
         status, lat, body = http_request(
             f"{self.base_url}/recall",
             payload={"session_id": probe_sid, "query": DEFAULT_RECALL_QUERY, "k": 1}, ...)
     else:
         # 无会话：recall 探针跳过（缺省 main 会创建幻影）
         status, lat, body = 0, 0.0, {}
     ```
  2. 字段改名：`mcp_orphan_count` → `orphan_count`（L244、L373 两处 + 相关 docstring/注释）。若下游仪表依赖旧名，可保留旧名作为别名（推荐直接改名并在变更说明注明）。
- **注意事项**：`DEFAULT_RECALL_QUERY` 常量保留；探针仍只读（/recall 不写缓冲）。
- **验证**：全新环境（无会话）跑 L2 → 无幻影会话产生；有会话时探针正常。

### [C13][2] `lms_ctl.sh:246-251` | start 失败仍触发 doubt 部署钩子
- **状态**：`[待实施]`
- **文件/段落**：`scripts/lms_ctl.sh` → 非 systemd 分支（L236）、怀疑钩子（L246-251）
- **怎么改**：
  1. `start) manual_start || exit 1 ;;`（L236）——manual_start 返回非零即失败退出，不再继续。
  2. 钩子改为只在**启动成功**后触发：
     ```bash
     # [C13] 只在启动/重启成功后才触发部署怀疑钩子（失败不记成成功事件）
     if [ "$ACTION" = "start" ]; then
         if manual_start; then
             _run_doubt_hook "start"
         else
             exit 1
         fi
     elif [ "$ACTION" = "restart" ]; then
         if manual_stop && manual_start; then
             _run_doubt_hook "restart"
         else
             exit 1
         fi
     fi
     ```
     （`_run_doubt_hook` 抽成函数包裹原 L247-251 逻辑；systemd 分支保持委派。）
- **注意事项**：PID 解析已按 cmdline 含 api.run 过滤（L82-92 实证），无需再改。
- **验证**：mock manual_start 失败 → 脚本 exit 1 且钩子未触发。

### [C14][2] `tools/migrate_gray_visible.py:7-8,37` | "前置：服务已停止"未强制
- **状态**：`[待实施]`
- **文件/段落**：`tools/migrate_gray_visible.py` → `main()` 入口（L21-23）
- **怎么改**：
  ```python
  def main():
      # [C14] fail-closed：live 运行时双写竞态 → 入口先探测 :8190，可达即退出
      import urllib.request
      try:
          with urllib.request.urlopen("http://127.0.0.1:8190/health", timeout=5) as r:
              if r.status == 200:
                  print("❌ LMS :8190 正在运行：灰池迁移会与 live 双写竞态，请先停服")
                  return 1
      except Exception:
          pass  # 不可达 → 允许执行
      ...
  ```
- **注意事项**：docstring"前置"改"强制前置（fail-closed）"。
- **验证**：live 运行中跑 → exit 1；停服后跑 → 正常。

### [C15][2] `tools/calibrate_per_dim_scale.py:63` | torch.cat 空样本崩溃
- **状态**：`[待实施]`
- **文件/段落**：`scripts/calibrate_per_dim_scale.py` → `main()`（L55-63）
- **怎么改**：
  ```python
  if not samples:
      print("无 per_dim_surprise 样本（全部为 None），跳过分布统计")   # [C15] 空样本守卫
      return
  all_vals = torch.cat(samples).numpy()
  ```
- **注意事项**：文件在 scripts/（审计写 tools/ 有误，以实际路径 scripts/calibrate_per_dim_scale.py 为准）。
- **验证**：跑一次（正常路径）；构造全 None 场景 → 打印提示不崩溃。

---

## 5. 汇总表

| Issue | 严重度 | 文件 | 状态 | 备注 |
|---|---|---|---|---|
| A1 | 5 | attractor.py | [待实施] | z 初始化 + 补断言 |
| A2 | 4 | bus_events.py | [待实施] | status() 内联 open 判定 |
| A3 | 4 | memory.py | [待实施] | 先截取后加固 |
| A4 | 3 | gap_registry.py | [待实施] | 统一 _same_case（覆盖 B7） |
| A5 | 3 | attractor.py | [待实施] | 深拷贝 events |
| A6 | 3 | attractor.py | [待实施] | 同步 j_target_norm |
| A7 | 3 | loop.py + attractor.py | [待实施] | learn entropy_correction 模式 |
| A8 | 2 | dream_engine.py | [待实施] | dream_cycle 补 consolidate |
| A9 | 2 | memory.py | [待实施] | 计数器自增 |
| A10 | 2 | loop.py + snapshot.py | [待实施] | last_activation 双侧持久化 |
| A11 | 2 | recovery.py | [待实施] | 回滚快照补 allostatic |
| A12 | 2 | loop.py | [待实施] | process_turn source 参数 |
| A13 | 2 | attractor.py | [待实施]（备选[需批准]） | 注释统一（主）；实现改 sqrt(2T)（备选） |
| A14 | 2 | dream_engine.py | [待实施] | _entry_id 文本哈希 |
| A15 | 1 | purpose.py | [待实施] | 注释修正 |
| A16 | 1 | loop.py | [待实施] | 前缀统一 + 快照 turn 对齐 |
| B1 | 4 | server.py | [待实施] | /chat/simple 补做梦锁 |
| B2 | 4 | session_manager.py | [待实施] | 全局快照归属校验 |
| B3 | 3 | server.py | [待实施] | /restore 归属+锁 |
| B4 | 3 | server.py | [待实施] | /snapshot 加锁 |
| B5 | 3 | server.py + dream_scheduler.py | [待实施] | acquire 移 run_in_executor |
| B6 | 3 | session_manager.py | [待实施] | 空 sid 告警 |
| B7 | 3 | gap_registry.py | [待实施] | = A4，不单独实施 |
| B8 | 3 | control.py | [待实施] | 假生效诚实标注（备选[需批准] reload 端点） |
| B9 | 3 | control.py | [待实施] | 注册 token 消费校验 |
| B10 | 3 | archive_store.py + archive_job.py | [待实施] | rebuild → 合并只增不删 |
| B11 | 3 | server.py | [待实施] | DELETE purge=true |
| B12 | 3 | config.py | [待实施] | _env_int/_env_float |
| B13 | 2 | wiring.py | [待实施] | 删除死代码 |
| B14 | 2 | server.py | [待实施] | meta 按增量条目列表 |
| B15 | 2 | server.py | [待实施] | 补 sid 别名 |
| B16 | 2 | translation_layer.py | [待实施] | embed 走熔断器 |
| B17 | 2 | gap_registry.py | [待实施] | RLock |
| B18 | 2 | clean_episodic.py | [待实施] | live 探测 fail-closed |
| B19 | 2 | session_manager.py | [待实施] | 会话级锁 |
| B20 | 2 | mcp_server.py | [待实施] | 快照路径钳制 |
| B21 | 1 | server.py | [待实施] | 删死代码 |
| B22 | 1 | llm_bridge.py + server.py | [待实施] | content or "" |
| B23 | 1 | control.py | [待实施] | 探针改只读端点 |
| C1 | 5 | lms_backup.sh | [待实施] | tar file-changed 放行 |
| C2 | 4 | lms_entropy_watchdog.py | [待实施] | 选蛋标准 + 隔离期 |
| C3 | 4 | watchdog + server.py + control.py | **[需批准]** | 新增 /reset-sigma（获批前只告警） |
| C4 | 4 | lms_entropy_watchdog.py | [待实施] | 判定顺序重构 |
| C5 | 4 | lms_entropy_watchdog.py | [待实施] | rc 检查 + 重启验证 |
| C6 | 3 | lms_ops_monitor.py | [待实施] | 孤儿判定排除 API |
| C7 | 3 | lms_dsh_feed.py | [待实施] | 水位停在已配对 seq |
| C8 | 3 | lms_dsh_feed.py | [待实施] | 上限语义修正 |
| C9 | 3 | lms_feed_docs_chunked.py | [待实施] | 硬切分 + 失败非零 |
| C10 | 2 | lms_http_mcp.py | [待实施] | 成功解析才清 buffer |
| C11 | 2 | lms_entropy_watchdog.py + crontab | [待实施]（crontab 为运维侧） | 去双日志 + 冷却 600s |
| C12 | 2 | lms_ops_monitor.py | [待实施] | 探针复用真实会话 + 字段改名 |
| C13 | 2 | lms_ctl.sh | [待实施] | start 失败即退出 |
| C14 | 2 | migrate_gray_visible.py | [待实施] | fail-closed 探测 |
| C15 | 2 | calibrate_per_dim_scale.py（scripts/） | [待实施] | 空样本守卫 |
| — | — | lms_status 嵌套读取 | `[已修复]`（08-22） | 存档 |
| — | — | fok 去重 | `[已修复]`（08-22） | 存档 |

---

## 6. 需 dandan 批准清单（`[需批准]`，设计进方案但不实施）

| 项 | 内容 | 理由 | 获批后动作 |
|---|---|---|---|
| **C3** | 新增数据面端点 `POST /reset-sigma/{session_id}` + 控制面 `POST /control/reset-sigma`（api/server.py、api/control.py、lms_entropy_watchdog.py 接线） | σ 重置必须作用于 live 进程（独立进程磁盘副本重置对 live 无效且并发写撕裂）；属**新增端点（血管级）**，红线要求批准 | watchdog 由"只告警"切换为端点调用 |
| **A13 备选** | `infer()` 扩散项实现改 `sqrt(2.0 * temperature)` | 改实现会改变噪声强度/温度标定（行为变化） | 若 dandan 要求物理正确性（FEP 标准 Langevin 形式）再实施 + 全量做梦回归 |
| **B8 备选** | 新增 `POST /control/reload-config` 转发端点（数据面重读 .env） | 新增端点（血管级）；当前已用"诚实标注"止血 | 若 dandan 要求热配置真生效再实施 |

> 其余 51 项均为非血管级修复（内部方法签名可选参数 / 逻辑修正 / 注释 / 脚本），**可直接实施**，不需要批准。

---

## 7. 实施协议（每个文件改完必须走完）

1. `cp <file> <file>.bak-$(date +%Y%m%d%H%M)`
2. `edit` 修改（带 issue 编号注释）
3. `python -m py_compile <file>`（.sh 用 `bash -n`）
4. 跑相关单测：`cd /vol2/1000/AI专用/living-memory-system-cloud && .venv/bin/python -m pytest tests/test_<相关>.py -q`（只跑受影响模块，全量可选）
5. 全模块完成后：`git add -A && git commit -m "fix(模块): 描述（issue A1/C1/...）"`（建议按 模块A/B/C 分批 commit）
6. 不重启 :8190 / :8191；C3 获批前 watchdog 不写盘

---

*本方案由 LMS 修复方案设计师生成，2026-08-23。单一事实来源：docs/audit_issues_20260823.md；实施进度以此文件状态列为准。*
