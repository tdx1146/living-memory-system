# R3（C1+C3）接线修复 —— 落盘与测试（2026-08-18）

> 指令：最后两个阻塞（900s 给图景直接写）——C1 labile 运行时接线 + C3
> P2-B 目的检查口径修正。输出 rewrite-ws/（已落盘），回复：改了什么 + 测试结果。

---

## 1. C1 labile 运行时接线（语义决策 D-2026-08-18-01 运行时落地）

**背景**：`core/doubt/reconsolidation_queue.py` 模块已在（27 例单测绿），但
`runtime/loop.py` 巩固期从未调用 `maybe_rewrite` —— 队列是"孤岛"，运行时
不参与。

**落盘**：`runtime/loop.py`

- **队列实例**（`__init__`）：`self.reconsolidation_queue = ReconsolidationQueue()`
  ——测试可注入 `reconsolidation_queue` 实例或 `reconsolidation_queue_path`
  （隔离路径）；缺省 env `LMS_DOUBT_RECONSOLIDATION_QUEUE_PATH` > 数据目录；
  开关 `LMS_DOUBT_RECONSOLIDATION_ENABLED`（默认 1）关 → 零参与。
- **巩固期三闸门调用**（`_reconsolidation_consolidate`，process_turn 内
  doubt_check 后、state_update 前——9 步固化不加步）：遍历 episodic 缓冲，
  对在队列中的条目调用 `maybe_rewrite(phase=CONSOLIDATION, rewrite_fn=写侧委托)`：
  - G1 候选在队 / G2 巩固期写侧时相 / G3 改写受控（`rewrite_fn` 委托
    `state_machine.consolidation_resolve`——写侧唯一转移入口之一；证据裁决：
    violated_by → superseded / 无证据 → kept / 超时 → downgraded；写侧默认保守）。
  - 任一闸门不过 → 不改写（机器防线）；返回本轮实际改写（全过）条目数。
- **写侧入队（只允许写侧时相）**：
  - commit 步注入 suspect 标记后登记（reason=`injection_suspect`，写侧入队即落盘）；
  - 体验层 D 去稳定化（mark_labile）后登记（reason=`destabilized_labile`）。
- **检索路径零改写保持**：/recall、/react 只读口不经过巩固期（不 process_turn
  则无再巩固）；队列只读接口绝不 setattr 条目——只读四不变不破坏。
- **观测与信号**：state_update 步观测 `reconsolidated`（本轮改写计数）；
  `_last_turn_reconsolidated`（上一轮再巩固是否真实发生）供目的时相消费。
- **claim 登记**：`runtime/loop.py` MODULE_CLAIMS + `runtime/claims.json`
  新增 `reconsolidation_wired_in_consolidation_phase`。

## 2. C3 P2-B 目的检查口径修正（只读轮 Q1 豁免）

**背景**：只读轮（无写侧活动的轮——如 /recall 查询）会被 Q1 判"否"→
drifted——但无写侧活动是只读语义，不是偏离（产出未流入活结构是误伤）。

**落盘**：`core/doubt/purpose_drift.py` + `runtime/loop.py`

- **只读轮信号**（purpose_drift.py）：`readonly_round(bool)` 或
  `round_type("readonly"/"write")`（显式字符串优先）。
- **Q1 豁免**：只读轮 → Q1 `state="exempt"`（ok=None，"不适用"不是"未判"
  也不是"否"）——不计入 verdict（drifted/aligned 判定排除 exempt）。
- **口径不放松**：写轮（缺省）无写侧活动 Q1 仍判"否"→ drifted（旧行为
  逐字节保持，test_purpose_drift.py 既有断言全绿）；豁免只作用 Q1，其他问
  判"否"仍 → drifted。
- **loop 接线**（`_purpose_drift_check`）：round_signals 新增
  `readonly_round = not (episodic_added or suspect_marked or 上一轮再巩固)`、
  `reconsolidated = _last_turn_reconsolidated`（再巩固真实发生 → Q1 判"是"，
  诚实信号——目的检查在 doubt_check 步、再巩固在其后，用上一轮结果）。
- **claim 同步**：`purpose_drift_gate_in_doubt_check` 语句补 P2-B 口径。

## 3. 测试（新增 + 全量回归）

| 测试集 | 结果 |
|---|---|
| tests/test_purpose_drift.py（新增 `TestReadonlyRoundExemption` 11 例：豁免/round_type/verdict 排除/不掩其他问否/drift_count 不计/无分数保证） | ✅ 57 passed |
| tests/test_loop_reconsolidation_labile.py（新增 12 例：队列实例/巩固期三闸门消化/检索零改写/写侧入队 spy/开关关零参与/目的信号接线/claim 一致） | ✅ 12 passed |
| tests/test_reconsolidation_queue.py（既有 27 例） | ✅ 27 passed |
| tests/test_doubt_m3.py + test_loop_m5.py（回归：9 步生命周期/四不变/验证链/怀疑状态机） | ✅ 112 passed |
| 定向集合（purpose_drift/reconsolidation_queue/doubt_m3/loop_m5/loop_reconsolidation_labile/api_server） | ✅ 235 passed |

全量回归：`pytest tests/ -q`（后台）→ 见文末结果行（预期基线 1373 + 新增
23 = 1396 passed）。
