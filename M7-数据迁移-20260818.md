# M7 数据迁移（2026-08-18）

规格：`四妹-LMS核心重写规格v2-20260817.md` §三（数据兼容与迁移：latest_main.pt v0.5.0 + main.jsonl 归档——幂等可回滚可重跑）/ §7.1 M7（新 schema 稳定后迁移、双写回滚、校验）
前置：M1（store 提取层）/ M2（recall 只读化）/ M3（doubt 原生）/ M4（allostatic J 原生并入）/ M5（loop 9 步固化）/ M6（dream 巩固）已完成——新 schema 稳定
工作区：rewrite-ws/ 继续（生产 venv：/tmp/repro3-1786556208/living-memory-system-cloud/.venv）

> 本会话续写说明：上次会话在迁移分析处超时（已识别硬复位段 turns 0-13/0-628——正是规格 §3.2-3 预判场景）；本次完成迁移全流程 + 真实数据演练 + 全套自验。

## 任务完成对照

| 任务 | 状态 |
| --- | --- |
| 1. 迁移脚本（快照 latest_main.pt v0.5.0 + main.jsonl 归档——合并去重——条目数校验——幂等可重跑——处理硬复位段特殊场景） | ✅ `tools/migrate_m7.py`（6 子命令：validate / migrate / rollback-drill / verify / dual-write；35 例单测全绿）+ 真实数据迁移完成 |
| 2. 回滚演练（备份可读性 + 中断重跑幂等——BK-01 教训） | ✅ 备份可读校验（快照 load + 归档解析 + 计数对账）+ 损坏备份检出 + 恢复 checksum 对账 + 中断重跑原子修复（真实数据实测，见下） |
| 3. 迁移后校验（recall 命中/不丢记忆/与 M1-M6 新 schema 兼容） | ✅ `verify_migrated` + 新核心加载闭环（MemoryManager.set_state → 444 条全字段齐备 → 语义检索自命中 miss 0） |
| 4. 自验全绿（生产 venv） | ✅ M7 42 例 + 全套回归 1149 passed（含 API 层 27；详见自验结果） |
| 5. 输出 rewrite-ws/（我落盘） | ✅ 本文件 + `tools/migrate_m7.py` + `tests/test_migration_m7.py` + 真实迁移产物 `data/migration/`（落盘清单见下） |

## 落盘文件清单

| 文件 | 内容 |
| --- | --- |
| tools/migrate_m7.py（**新增**，本会话修 1 处） | M7 迁移工具（纯 stdlib + torch；§3.2 六步 1:1 落地）：`validate_snapshot`（条目数/字段完整性/confidence∈[0,1]/turn 单调性——硬复位归 0 判 warning 不阻断、非复位乱序判 error 阻断）/ `validate_archive_records`（行级字段）/ `map_entry_to_new`（旧字段→新字段：rebuttal_consistency 写侧初始化、怀疑态 stable、violated_by→superseded、store_gray 原样保留+gray 归一、confidence 钳制、last_reinforced_turn=写入 turn）/ `merge_entries`（快照∪归档按 (turn,text_hash) 去重，快照优先；条目数公式 + turn 序列完整性）/ `backup_inputs`（BK-01 可读校验）/ `rollback_drill`（可读+恢复对账）/ `run_migrate`（0 备份→1 校验→2/3 映射合并→4 落盘→5 双写 journal，原子替换，dry-run 零写入）/ `verify_migrated`（recall 自命中/灰度与 [doubt] 排除/归档可检索/计数对账）/ `dual_write_record/check`（§3.2-5 增量一致 journal，不一致或轮数不足禁止切单写）——env 化默认值（LMS_M7_*，docstring 唯一权威） |
| tests/test_migration_m7.py（**新增**） | M7 单测 35 例：快照校验（含 confidence 超界/乱序 error）/ 归档双格式+坏行 fail-open / 合并去重公式+快照优先+turn 序列完整性 / schema 映射（新字段初始化/钳制/灰度归一/证伪 superseded）/ 备份可读+损坏检出+恢复对账 / 中断重跑幂等+dry-run 零写入+校验阻断 / 迁移后校验（自命中/灰度 [doubt] 排除/归档可检索/空快照检出）/ 双写 journal 四态 / 端到端（迁移产物被新核心加载+recall） |
| tests/test_episodic_migration.py（M7 相关，前次落盘） | 7 例：旧快照条目 getattr 兜底 / 新字段初始化 / 写侧加固引用 / set_state 往返混合新旧条目 |
| data/migration/inputs/ | **输入只读参考层**（§3.3 无删除、只增不改）：latest_main.pt（v0.5.0 turn-629 干净基线，419 条）+ main.jsonl（165 行归档）——sha256 见下 |
| data/migration/backups/&lt;ts&gt;/ | 迁移备份（双份之一，BK-01 可读校验通过）：latest_main.pt + main.jsonl + backup_meta.json |
| data/migration/main/ | **迁移产物（规范）**：migrated_latest_main.pt（444 条新 schema 快照）+ main.jsonl（重建归档）+ manifest.json |
| data/migration/main-rerun/ | 幂等重跑证据（同一输入重跑输出与 main/ 字节一致） |
| data/migration/rollback-restore/ | 回滚恢复演练证据（恢复文件与输入逐字节一致） |
| data/migration/new_storage/ | 当前新存储备份（§3.2-0"当前新存储"：latest_default.pt + default.jsonl） |
| M7-数据迁移-20260818.md（本文件） | 交付说明 |

## 真实数据迁移结果（§3.2 全流程实测）

输入：`latest_main.pt`（v0.5.0，turn-629，episodic 419，ts=1786851771=8/16 11:42 干净基线）+ `main.jsonl`（165 行，坏行 0，8/10-8/18 归档）。

```
0 备份     data/migration/backups/20260818_1243xx/ —— 可读校验通过（快照 419 / 归档 165）
1 校验     快照：419 条目；字段齐；confidence 全在 [0,1]；turn 单调性——
           entry[14] 13→0 硬复位段（规格 §3.2-3 预判场景）→ warning 不阻断
           归档：165 行全有效（坏行 0）
2 映射     419 快照条目 → 新 schema（rebuttal_consistency 写侧 'ingest'、
           怀疑态 stable、last_reinforced_turn=turn、source 原样含 store_gray）
3 合并     快照 419 + 归档 165 - 去重 140（与快照重复 140）= 444（公式 OK；
           turn 非降、键唯一双重校验过）
           归档唯一新增 25 条（8/16 后增量，turns 629-846——不丢记忆）
4 落盘     migrated_latest_main.pt（turn_count=846=max(629, 合并 max turn)；
           timestamp=原数据时基→重跑字节级可复现）+ main.jsonl + manifest.json
           快照 sha256 a1b691acc1b01abb… / 归档 sha256 14e996694481c998…
```

硬复位段处理（本会话续写核心项）：真实输入快照含 turns 0-13 与 0-628 两段（复位归 0）；归档另含多段复位（0-13 / 0-117 / 29-132 / 0-846）。按规格 §3.2-3，**输入侧**单调性对归 0 复位段只 warning（不阻断——真实数据现实），**合并输出侧**严格强制 turn 非降 + (turn,text_hash) 键唯一（error 级，实测过）。非复位乱序（如 5→3）判 error 阻断（数据损坏信号）。

## 回滚演练结果（② + BK-01）

| 演练 | 结果 |
| --- | --- |
| 备份可读性（快照 load + 归档解析 + 计数对账） | ✅ 通过（419 / 165 与 backup_meta 登记一致） |
| 损坏备份检出（截断快照 / 全坏行归档 → error） | ✅ 单测覆盖（test_corrupted_backup_detected / test_corrupted_archive_backup_detected） |
| 恢复演练（备份 → restore_to + checksum 对账） | ✅ 恢复文件与原输入逐字节一致（5741fbf7… / 12ef5d37…） |
| 中断重跑幂等（真实数据） | ✅ 重跑输出 sha256 与首跑一致（快照/归档双文件）；输出快照截断 1/3 后重跑 → 原子修复回原 checksum，条目 444 不重复不丢失 |

## 迁移后校验结果（任务③）

`verify_migrated`（迁移产物 + 新核心加载闭环，实测）：

- 新核心加载迁移快照：episodic = 444（与合并条目数一致——不丢记忆）；
- recall 自命中：抽样 30 条，miss 0（语义检索逐条命中自身）；
- 灰度口径：store_gray 284 条，external 检索面泄漏 0（L1 不可召回口径保持）；
- [doubt] 排除：1 条，external 面泄漏 0（P0 污染处置语义保持）；
- 归档可检索：归档条目数 = 444；
- 新 schema 兼容：444 条全含 doubt_state / rebuttal_consistency / last_reinforced_turn / info_value / core / ts / gray / violated_by / reference_count / confidence；doubt_state 分布 {stable: 444}（真实数据无旧证伪记录）；
- turn 序列：非降、键唯一 0 重复；turn_count=846 续写点正确（max(629, 846)）。

## 自验结果（生产 venv：/tmp/repro3-1786556208/living-memory-system-cloud/.venv）

- `pytest tests/test_migration_m7.py tests/test_episodic_migration.py -q` → **42 passed**（M7 35 + 迁移兼容 7）
- `pytest tests/ -q --ignore=tests/test_api_server.py` → **1122 passed**（M1-M6 全套无回归）
- `pytest tests/test_api_server.py -q` → **27 passed**（API 层血管契约）
- 合计 **1149 passed**（生产 venv，Python 3.11.2，torch 2.13.0+cu130，pytest 9.1.1）

## 本会话改动清单

| 文件 | 变更类型 | 内容 |
| --- | --- | --- |
| tools/migrate_m7.py | 修改（1 处） | `validate_snapshot` turn 单调性分级：**归 0 硬复位段 → warning（不阻断，真实数据 13→0 场景）；非复位乱序（5→3）→ error（阻断）**——原实现把两类都降为 warning 导致单测 `test_turn_monotonicity_violation_is_error` 红；修正后 35 例全绿且真实数据迁移不被误阻断（复位段段内继续单调校验） |
| M7-数据迁移-20260818.md | 新增 | 本文件 |

## 生产切换说明（§3.2 步骤 5-6，运维侧）

1. 迁移产物已就绪：`rewrite-ws/data/migration/main/migrated_latest_main.pt` + `main.jsonl`（manifest 齐备）；
2. 切换前把 `LMS_M7_DUAL_WRITE_ROUNDS` 设为 N>0 进入双写回滚期：每轮记录新旧增量（`dual-write record`），N 轮一致（`dual-write check --rounds N`）后才切单写；任一不一致 → 禁止切单写；
3. 任一步校验失败 → 用 `data/migration/backups/&lt;ts&gt;/` 回滚（`rollback-drill --restore-to` 已演练）；
4. 旧输入（`data/migration/inputs/`）保留为只读参考层，全程无删除（§3.3 不丢记忆铁律）。
