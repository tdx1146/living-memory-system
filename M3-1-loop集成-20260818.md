# M3-1 loop 集成说明（2026-08-18）

规格：四妹-LMS核心重写规格v2-20260817.md §2.1/§2.2/§2.3/§5.2
状态：tests/test_doubt_m3.py + tests/test_doubt_integration.py 全绿（66 passed）；全套 1014 passed / 0 failed（生产 venv：/tmp/repro3-1786556208/living-memory-system-cloud/.venv）。

## 落盘文件清单（本次 loop 集成涉及的改动/新增）

| 文件 | 内容 |
| --- | --- |
| runtime/loop.py | ① `__init__` 实例化 `self.doubt_state = DoubtStateMachine(verification_chain=self.verification_chain)`（loop.doubt_state 属性——6 failed 的 AttributeError 根因）；② `process_turn` 写侧注入时怀疑（store_episodic 后 `injection_check`，fail-open）；③ `recall_episodic_readonly` / `react_readonly` 检索后经 `doubt_state.retrieval_projection` 进内存态 labile 窗口（衔接 M2 suspicion 投影）；④ `get_status` 增 `doubt_native` 观测块 |
| core/doubt/state_machine.py | 三时相怀疑状态机（检索时只读投影 / 注入时 suspect / 巩固时 resolve_labile 接口；labile 内存态不落库；状态转移仅写侧驱动） |
| core/doubt/rebuttal_field.py | rebuttal-consistency 字段原生（§2.3 存取接口 + updated_by 写者守卫铁律） |
| core/doubt/verification_chain.py | 验证链原生骨架（§2.2 幂等键协议 / 防伪独立四维 / 开关默认关） |
| core/doubt/__init__.py + claims.json | MODULE_CLAIMS 机器验证登记（§5.2） |
| core/doubt/reconsolidation.py | 去稳定化 mark_labile 同步原生字段（写侧溯源） |
| tests/test_doubt_m3.py | M3-1 单测 40 例（含 TestLoopIntegrationM3 6 例——原 6 failed 修复验证） |
| tests/test_doubt_integration.py | 体验层 D 集成 26 例（回归绿） |

## 失败修复点（6 failed → 66 passed）

1. **loop.doubt_state AttributeError（根因）**：loop.py `__init__` 缺状态机实例化。
   修复：实例化 `DoubtStateMachine(verification_chain=VerificationChain())`，
   暴露 `loop.doubt_state` / `loop.verification_chain` 属性。
2. **写侧注入时怀疑未接线**：`process_turn` 只在 `store_episodic` 实际新增条目后
   （条目数变化判定，跳过垃圾过滤）调 `doubt_state.injection_check`——
   高 surprise（>factor×J_target）或 rebuttal 命中 → 标 suspect + 验证链登记；
   fail-open，异常只告警不阻断主循环。
3. **检索时 labile 窗口未接线**：`recall_episodic_readonly` / `react_readonly`
   检索后调 `doubt_state.retrieval_projection(scored, precision_adapt=..., consistency_provider=...)`——
   命中条目进内存态 labile 窗口（绝不 setattr，只读四不变守卫包裹），
   投影结构与 M2 project_suspicion 逐键同构（/recall、/react suspicion 区段形态冻结）。
4. **观测缺位**：`get_status` 增 `doubt_native` 块（enabled / 三时相计数 / 验证链快照）。

## 自验结果

- `pytest tests/test_doubt_m3.py tests/test_doubt_integration.py -q` → **66 passed**
  - TestThreePhaseStateMachine 16 / TestRebuttalConsistencyField 9 /
    TestVerificationChainSkeleton 9 / TestLoopIntegrationM3 6（原 6 failed 全部转绿）+
    test_doubt_integration.py 26
- 全套回归 `pytest tests/ -q`（含 test_api_server.py）→ **1014 passed / 0 failed**
- 关键红线复核：检索时相零条目改写（`test_retrieval_phase_never_mutates_entry`）、
  状态转移仅写侧（`test_transitions_only_from_write_side`）、
  验证链开关默认关（`test_switch_default_off`）、
  recall 带原生字段后四不变仍成立（`test_recall_readonly_four_invariants_hold_with_native`）。
