# M5 loop 重组（2026-08-18）

规格：四妹-LMS核心重写规格v2-20260817.md §1.2（loop.py：turn 生命周期 process_turn 唯一增量点；生命周期固定 9 步，禁止旁路；状态管理清单）/ §7.1 M5（9 步生命周期固化、状态管理清单落地）
前置：M4 已落盘（allostatic J 原生并入 attractor，1060 passed）；M1（store 提取层）/ M2（recall 只读化）/ M3（doubt 原生）已完成
工作区：rewrite-ws/ 继续（生产 venv：/tmp/repro3-1786556208/living-memory-system-cloud/.venv）

## 任务完成对照

| 任务 | 状态 |
| --- | --- |
| 1. 9 步生命周期固化（§1.2/§7.1 M5——turn 生命周期 9 步清晰化：ingest→encode→query→retrieve→integrate→doubt_check→commit→state_update→emit，末步状态汇总） | ✅ runtime/lifecycle.py TURN_LIFECYCLE_9 + process_turn 9 步显式化 + 每轮状态汇总 |
| 2. 状态管理清单（§1.2 表：turn 计数唯一增量点/写侧统一入口/只读四不变衔接/怀疑状态机衔接——M1-M4 集成收口） | ✅ runtime/lifecycle.py STATE_OWNERSHIP（六项 × 唯一写者）+ _increment_turn 唯一增量点 + tests 机器验证 |
| 3. 塑形输入预处理归位（extract_core 塑形归入 loop——§7.1 M5 相关——**如规格有**） | ✅ **规格 v2 无此要求**（extract_core 全规格 0 提及）→ 负面发现登记，不跨模块改（见下） |
| 4. 自验：rewrite-ws 跑相关测试全绿（生产 venv），重点无回归（M1-M4 测试也跑） | ✅ M5 34 + M1-M4 相关 240 + 全套回归（见下） |
| 5. 输出 rewrite-ws/ | ✅ 本文件 + 改动清单 |

## 落盘文件清单

| 文件 | 内容 |
| --- | --- |
| runtime/lifecycle.py（**新增**） | M5 机器可读交付（纯 stdlib，防循环依赖）：① `TURN_LIFECYCLE_9`——9 步固化清单（规格 §1.2 步名/语义 + 代码锚点 + 写者/读者）；② `STATE_OWNERSHIP`——状态管理清单（规格 §1.2 表：六项状态 × 归属模块/唯一写者/只读读者/验证锚点）；③ `LifecycleTrace`——每轮生命周期记录器（纯内存，重启即失；未知步/重复步 → 拒绝并记 `_rejected` → 汇总 broken=True——机器可验红线） |
| runtime/claims.json（**新增**） | loop 语义 claim 登记（§5.2 machine-readable）：唯一增量点 / 9 步无旁路 / 状态管理清单单一写者 / 记录器零副作用 / 四不变衔接——与 loop.py MODULE_CLAIMS 同源 |
| runtime/loop.py | ① 顶层 `MODULE_CLAIMS`（与 claims.json 同源）；② `__init__` 增 `lifecycle_trace`/`last_turn_lifecycle`（纯内存，不落盘不进快照）；③ `_increment_turn()`——**turn 计数唯一增量点**（全库唯一 `self.turn_count += 1` 站点，grep 可证）；④ `_record_lifecycle()`——生命周期观测记录（fail-open，G 模式以日志可见）；⑤ `process_turn` 9 步显式化（每步 banner + 观测记录；`_epi_before` 基准外提为 commit 观测口径，行为逐字节不变）；⑥ `get_status` 增 `lifecycle` 观测块（§4.2 独立追加语义，纯增量字段） |
| tests/test_loop_m5.py（**新增**） | 34 例：清单（9 步/六项单一写者/claims 同源）/ 唯一增量点（含静态 grep 验证 + 只读口零增量 + load 恢复非增量）/ 9 步无旁路（broken 判定）/ 状态汇总结构 / 记录器零副作用（json 可序列化、只读口无记录）/ 只读四不变在 M5 后仍成立 / get_status 观测块（C-01 必需键不变）/ M1-M4 集成收口（commit 写侧统一入口真实落库 + [doubt] 跳过 + 怀疑状态机计数 + allostatic 观测键） |

## 设计要点（对照规格 §1.2/§7.1）

- **9 步固化 = 规格步名 + 机器可验无旁路**：`TURN_LIFECYCLE_9` 按规格 §1.2 规范序登记（ingest/encode/query/retrieve/integrate/doubt_check/commit/state_update/emit）；process_turn 每轮把每步执行观测记录进 `LifecycleTrace`，状态汇总（`last_turn_lifecycle`）判定 `broken`（missing/duplicated/unknown 任一非空即 broken=True——禁止旁路的机器防线）；测试断言每轮 9 步各恰一次。
- **执行序说明（诚实登记，不造假序）**：`execution_order` 是 FEP 算法序（state_update 的锚点分散在管线：learn/π/σ 更新须在检索前完成——算法依赖不能挪动；doubt_check 的应用锚点在 commit 之后），`spec_order` 是规格序；汇总**按规格序呈现**，两序同时暴露供审计。
- **状态管理清单落地（M1-M4 集成收口）**：`STATE_OWNERSHIP` 六项 × 唯一写者（turn 计数=process_turn / J·工作点·σ=state_update / π=doubt ingest·consolidation / entry.confidence=store 注入·巩固 / entry 怀疑态=doubt 三时相状态机（写侧）/ fok_unresolved=验证链）；`_increment_turn` 是全库唯一 `+= 1` 站点（测试静态 grep 验证；load_state 快照恢复 = 赋值恢复非增量，已登记说明）；commit 步观测写侧统一入口真实落库（episodic_delta/entry_added/suspect_marked/sys_event_skipped）；doubt_check 步观测怀疑状态机计数（injection_checks/suspect_marked——M3 衔接）；state_update 步观测 J/σ/π 增量 + allostatic_events（M4 衔接——§1.3 emit 落观测同源）。
- **记录器零副作用**：LifecycleTrace 只读观测（标量，json 可序列化——测试断言不泄漏条目对象/张量）、绝不 setattr 条目、绝不写持久层、不进快照；`lifecycle_trace` 仅在 process_turn 执行期间非 None（/recall、/react、/dream 恒 None——测试断言）。
- **观测全为纯增量**：get_status `lifecycle` 块与 M3/M4 的 `doubt_native`/`allostatic_j` 同款 §4.2 独立追加语义；C-01 必需键（turn_count/entropy_ratio/purpose_coherence）不变（测试断言）。
- **一次一变量**：未触碰 attractor/doubt/store/recall/api 的任何算法；process_turn 仅加记录点 + 基准外提（读操作，行为逐字节不变）；`runtime/allostatic_j.py` 外挂未复活。

## 塑形输入预处理归位——负面发现登记（任务 3）

- **规格核查**：`四妹-LMS核心重写规格v2-20260817.md` 全文 **0 次**提及 `extract_core`（grep 证实）；任务书所述"规格提到 extract_core 的塑形归入 loop"在规格 v2 中**不存在**（"如规格有"条件不成立）。
- **现状核实（不改）**：`api/server.py` /feed 端点（L807-831）在 **api 层**做塑形预处理（`extract_core(req.llm_output)` → ≤300 字核心），以 `llm_output` 参数喂入 `loop.process_turn(req.user_input, core)`——loop 的 ingest 步已接收预处理后的输入；extract_core 是 api 层薄工具（血管），移入 loop 违反"血管不换"且规格无依据。
- **处置**：按"禁止跨模块顺手改"原则**不动 extract_core、不动 api/server.py**；本负面发现在此登记，若后续规格 v2 修订补充该要求，再按 M8 契约评审推进。

## 自验结果

生产 venv：`/tmp/repro3-1786556208/living-memory-system-cloud/.venv`（Python 3.11.2，torch 2.13.0+cu130，pytest 9.1.1）

- `pytest tests/test_loop_m5.py -q` → **34 passed**（M5 新增）
- `pytest tests/test_loop_m5.py tests/test_store_m1.py tests/test_recall_m2.py tests/test_recall_readonly.py tests/test_doubt_m3.py tests/test_doubt_integration.py tests/test_allostatic_j.py tests/test_react_readonly.py -q` → **240 passed**（M5 + M1-M4 相关无回归）
- 全套 `pytest tests/ -q` → **见下（后台全量回归，收尾时回填）**

## 改动清单（模块/文件/claim 变更/契约影响）

| 文件 | 变更类型 | claim 变更 | 契约影响 |
| --- | --- | --- | --- |
| runtime/lifecycle.py | 新增 | —（新模块） | 无（纯增量，未接线对外契约） |
| runtime/claims.json | 新增 | loop 5 条 claim 登记 | 无 |
| runtime/loop.py | 修改 | 新增 MODULE_CLAIMS（5 条）+ `_increment_turn`（唯一增量点 claim 落点） | get_status 增 `lifecycle` 纯增量字段（§4.2 独立追加语义；C-01 必需键不变）；端点签名/返回结构零改动 |
| tests/test_loop_m5.py | 新增 | — | 无（测试文件） |
